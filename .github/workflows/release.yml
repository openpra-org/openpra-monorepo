name: Nx Release

on:
  workflow_dispatch:
    inputs:
      dryRun:
        description: "Run release in dry-run mode"
        required: false
        default: "true"
  push:
    branches:
      - main

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write
    services:
      mongo:
        image: mongo:6
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --eval 'db.adminCommand(\"ping\")'" --health-interval 10s --health-timeout 5s --health-retries 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Ensure pnpm available
        run: npx -y pnpm@10.19.0 --version

      - name: Install dependencies (skip lifecycle scripts)
        run: npx -y pnpm@10.19.0 install --frozen-lockfile --ignore-scripts

      - name: Install system dependencies for native build
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            make \
            cmake \
            g++ \
            git \
            python3 \
            curl \
            ca-certificates \
            libxml2-dev \
            libomp-dev \
            libgoogle-perftools-dev \
            libboost-program-options-dev \
            libboost-math-dev \
            libboost-random-dev \
            libboost-filesystem-dev \
            libboost-test-dev \
            libboost-date-time-dev \
            libjemalloc-dev \
            libxml2 \
            libboost-filesystem1.74.0 \
            libboost-program-options1.74.0 \
            libtcmalloc-minimal4 \
            libjemalloc2

      - name: Build scram-node native addon
        run: |
          cd apps/solvers/scram
          npx -y pnpm@10.19.0 run build

      - name: Install scram CLI (optional)
        run: |
          if [ -f apps/solvers/scram/build/cmake_install.cmake ]; then
            sudo cmake --install apps/solvers/scram/build
            sudo ldconfig
          else
            echo "No CMake install script found; skipping scram-cli install"
          fi

      - name: Build
        # env:
        #   NX_CLOUD_ACCESS_TOKEN: ${{ secrets.NX_CLOUD_ACCESS_TOKEN }}  # (disabled)
        #   NX_BRANCH: ${{ github.ref_name }}                             # (disabled)
        #   NX_RUN_GROUP: release-${{ github.run_id }}                    # (disabled)
        run: npx -y pnpm@10.19.0 nx run-many -t build --no-cloud

      - name: Test native addons
        env:
          CI: "true"
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          export LD_LIBRARY_PATH="/usr/local/lib:/usr/local/lib64:$HOME/.local/lib/scram:${LD_LIBRARY_PATH:-}"
          npx -y pnpm@10.19.0 nx run solvers-praxis-node:test --no-cloud
          npx -y pnpm@10.19.0 nx run engine-scram:test --no-cloud

      - name: Test services and applications
        env:
          CI: "true"
          MONGO_URI: mongodb://localhost:27017/test
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          export LD_LIBRARY_PATH="/usr/local/lib:/usr/local/lib64:$HOME/.local/lib/scram:${LD_LIBRARY_PATH:-}"
          npx -y pnpm@10.19.0 nx run praetor:test --no-cloud
          npx -y pnpm@10.19.0 nx run-many -t test --projects=backends-web-backend,frontends-web-frontend,interfaces-shared-types --no-cloud -- --testTimeout=60000

      - name: Nx Release Version (Conventional Commits)
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.dryRun == 'true' }}
        run: npx -y pnpm@10.19.0 nx release version --dry-run

      - name: Nx Release Version (apply)
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.dryRun != 'true' }}
        run: npx -y pnpm@10.19.0 nx release version

      - name: Nx Release Changelog
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.dryRun != 'true' }}
        run: npx -y pnpm@10.19.0 nx release changelog

      - name: Nx Release Publish (npm)
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.dryRun != 'true' }}
        env:
          NPM_CONFIG_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npx -y pnpm@10.19.0 nx release publish

      - name: Create GitHub Release
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.dryRun != 'true' }}
        run: npx -y pnpm@10.19.0 nx release github
