import { type SRReference } from "interfaces-mef-types/core/pra-common";
import {
  INTERNAL_FIRE_PRA_SR_CATALOG,
  type InternalFireAnalysisRecord,
  type InternalFirePRA,
  type InternalFirePraInterfaceRecord,
  type InternalFirePraInterfaceTransferItem,
  type InternalFirePraSubelement,
  type InternalFireProcessDocumentation,
} from "interfaces-mef-types/internal-fire/internal-fire-pra";
import { synchronizeInternalFirePraDerivedRegisters } from "interfaces-mef-types/internal-fire/internal-fire-pra-validation";
import { createBlankInternalFirePra } from "../../internal-fire-pra-workbooks/blank-internal-fire-pra";
import { createHazardConditionedMethodModels } from "./hazard-conditioned-method-model-seed";

export type InternalFirePraVariant = "htgr" | "sfr";
interface PlantContext {
  workbookName: string; plantName: string; reactorType: string; thermalPower: string; primaryCoolant: string; modules: number;
  site: string; baselineModel: string; buildings: string[]; systems: string[]; equipment: string[]; principalMetric: string;
}
const CONTEXT: Record<InternalFirePraVariant, PlantContext> = {
  htgr: {
    workbookName: "F Workbook 1", plantName: "Generic Prismatic HTGR Unit 1", reactorType: "Four-module prismatic high-temperature gas-cooled reactor",
    thermalPower: "600 MWt per module", primaryCoolant: "Helium", modules: 4, site: "Cedar Basin Site", baselineModel: "HTGR Full-Scope PRA Model R6.2",
    buildings: ["Reactor Building", "Reactor Service Building", "Essential Services Building", "Control Building", "Turbine Building", "Radwaste Building", "Fuel Handling Building", "Diesel Generator Building"],
    systems: ["Reactor trip", "Shutdown cooling", "Core heat removal", "Essential AC", "125 VDC", "Reactor building HVAC", "Cavity cooling", "Spent-fuel cooling"],
    equipment: ["RPS-DIV-A", "SCS-P-1A", "CHR-BLW-A", "4.16KV-SWG-A", "DC-PNL-A", "RB-HVAC-FAN-A", "RCCS-DMP-A", "SFP-P-A"], principalMetric: "Aggregate release-category frequency per plant-year",
  },
  sfr: {
    workbookName: "F Workbook 2", plantName: "Generic Pool-Type SFR Unit 1", reactorType: "Two-unit pool-type sodium-cooled fast reactor",
    thermalPower: "840 MWt per unit", primaryCoolant: "Liquid sodium", modules: 2, site: "Pioneer Mesa Site", baselineModel: "SFR Full-Scope PRA Model R5.4",
    buildings: ["Reactor Building", "Steam Generator Building", "Decay Heat Removal Building", "Control Building", "Fuel Handling Building", "Balance-of-Plant Building", "Electrical Building", "Sodium Service Building"],
    systems: ["Reactor protection", "Direct reactor auxiliary cooling", "Intermediate heat transport", "Essential AC", "125 VDC", "Containment HVAC", "Sodium leak detection", "Spent-fuel cooling"],
    equipment: ["RPS-DIV-A", "DRACS-DHX-A", "IHTS-P-A", "4.16KV-SWG-A", "DC-PNL-A", "CONT-HVAC-FAN-A", "SOD-LEAK-PNL-A", "SFP-P-A"], principalMetric: "Aggregate release-category frequency per plant-year",
  },
};

function id(variant: InternalFirePraVariant, code: string): string { return `${variant}-${code.toLowerCase().replace(/[^a-z0-9]+/g, "-")}`; }
function scientific(value: number): string { return value.toExponential(2).replace("e", "E"); }
function titleCaseForSeed(value: string): string { return value.toLowerCase().replace(/_/g, " ").replace(/\b\w/g, (letter) => letter.toUpperCase()); }
function reference(sr: string): SRReference { const entry = INTERNAL_FIRE_PRA_SR_CATALOG[sr]; if (entry === undefined) throw new Error(`Unknown Internal Fire requirement ${sr}`); return { sr, hlr: entry.hlr }; }
function record(variant: InternalFirePraVariant, code: string, name: string, description: string, basis: string, evidenceRefs: string[] = []): InternalFireAnalysisRecord {
  return { uuid: id(variant, code), code, name, description, basis, owner: "Internal Fire PRA Team", status: "APPROVED", evidenceRefs, relatedRefs: [], assumptionRefs: [], implementsSrs: [] };
}
function cover(records: InternalFireAnalysisRecord[], prefix: InternalFirePraSubelement): void {
  Object.keys(INTERNAL_FIRE_PRA_SR_CATALOG).filter((sr) => sr.startsWith(`${prefix}-`)).forEach((sr, index) => records[index % records.length]?.implementsSrs.push(reference(sr)));
}
function documentation(variant: InternalFirePraVariant, prefix: InternalFirePraSubelement, name: string, process: string, inputs: string, methods: string, results: string): InternalFireProcessDocumentation {
  return { uuid: id(variant, `DOC-${prefix}`), name, processDescription: process, inputsDescription: inputs, methodsDescription: methods, resultsDescription: results,
    limitations: ["Pre-operational as-built routing and final fire-protection turnover information remain controlled closure items."],
    supportingDocumentRefs: [id(variant, "EVID-GA"), id(variant, "EVID-CABLE"), id(variant, "EVID-WALK")],
    traceabilityLinks: Object.keys(INTERNAL_FIRE_PRA_SR_CATALOG).filter((sr) => sr.startsWith(`${prefix}-`)).map((sr) => ({ uuid: id(variant, `TRACE-${sr}`), requirementRef: sr, inputRefs: [id(variant, "EVID-GA")], modelRefs: [id(variant, `${prefix}-MODEL`)], resultRefs: [id(variant, `${prefix}-RESULT`)], documentationRefs: [id(variant, `DOC-${prefix}`)] })),
  };
}
function uncertainty(variant: InternalFirePraVariant, prefix: InternalFirePraSubelement, affected: string[]): InternalFirePRA["integratedUncertainties"][number] {
  return { ...record(variant, `${prefix}-UNC-001`, `${prefix} dominant model uncertainty`, `Alternative treatments for the leading ${prefix} assumption can change retained scenario frequency or conditional consequence.`, "Nominal, conservative, and bounding alternatives are propagated in the controlled sensitivity set.", [id(variant, "EVID-DATA")]), sourceSubelement: prefix, uncertaintyType: "MODEL", affectedRecordRefs: affected, potentialImpact: "May change the leading contributor by approximately 10 to 35 percent without changing the principal risk conclusion.", reasonableAlternatives: ["Nominal best estimate", "Conservative alternative", "Bounding no-credit case"], treatment: "Explicit sensitivity plus integrated uncertainty propagation", sensitivityStudyRefs: [id(variant, `FESQ-SENS-${prefix}`)], importance: "HIGH" };
}
function preop(variant: InternalFirePraVariant, prefix: InternalFirePraSubelement, affected: string[]): InternalFirePRA["plantBoundaryAndPartitioning"]["preOperationalAssumptions"][number] {
  return { ...record(variant, `${prefix}-PREOP-001`, `${prefix} as-built confirmation`, `Final as-built and as-operated information supporting ${prefix} will be confirmed before fuel-load readiness.`, "The design baseline is conservative and the closure action is tracked under configuration control.", [id(variant, "EVID-CONFIG")]), affectedRecordRefs: affected, missingDesignInformation: ["Final turnover revision", "As-built walkdown confirmation"], limitation: "Current result uses a conservative pre-operational representation.", closureAction: "Complete turnover walkdown, reconcile the controlled database, and requantify if the acceptance criterion is exceeded.", closurePhase: "Fuel-load readiness", closureStatus: "IN_PROGRESS" };
}

export function createInternalFirePraExample(variant: InternalFirePraVariant): InternalFirePRA {
  const context = CONTEXT[variant];
  const mef = createBlankInternalFirePra(context.workbookName, "Internal Fire PRA Team");
  mef.hazardConditionedModels = createHazardConditionedMethodModels("FIRE", "Internal fire");
  mef.uuid = id(variant, "INTERNAL-FIRE-PRA"); mef.version = "1.0"; mef.plantStage = "PRE_OPERATIONAL";
  mef.praScope = `${context.plantName} Internal Fire PRA for all defined at-power operating states, ${String(context.modules)} reactor modules or units, shared support systems, spent-fuel inventory, and other modeled radioactive-material sources within the licensee-controlled area.`;
  mef.metadata.scope = mef.praScope; mef.metadata.plantIdentity = { name: context.plantName, vendor: "OpenPRA reference design", reactorType: context.reactorType, thermalPower: context.thermalPower, primaryCoolant: context.primaryCoolant, siteName: context.site, numberOfModules: context.modules };
  mef.metadata.limitations = ["Final cable routing turnover remains open for selected low-voltage control circuits.", "Plant-specific operating fire experience will replace generic exposure after sufficient commercial operation."];
  mef.metadata.reviewers = [
    { id: id(variant, "REVIEWER-FIRE-PROTECTION"), name: "Dr. Lena Ortiz", role: "INTERNAL_REVIEWER", organization: "OpenPRA Reference Design Authority", title: "Lead Technical Reviewer · Fire Protection" },
    { id: id(variant, "REVIEWER-ELECTRICAL"), name: "Marcus Ellison", role: "INTERNAL_REVIEWER", organization: "OpenPRA Reference Design Authority", title: "Independent Reviewer · Electrical and Circuit Analysis" },
    { id: id(variant, "REVIEWER-HRA"), name: "Priya Raman", role: "INTERNAL_REVIEWER", organization: "OpenPRA Reference Design Authority", title: "Independent Reviewer · Fire HRA" },
    { id: id(variant, "APPROVER-RISK"), name: "Dr. Ji-won Chen", role: "INTERNAL_APPROVER", organization: "OpenPRA Reference Design Authority", title: "Director · Risk Engineering", qualification: "Qualified PRA technical authority and NQA-1 lead reviewer" },
  ];
  const reviewComments = [
    { uuid: id(variant, "COMMENT-01"), authorRole: "INTERNAL_REVIEWER" as const, authorId: id(variant, "REVIEWER-ELECTRICAL"), createdAt: "2026-07-08T14:15:00Z", associatedSr: "FCS-A3", associatedField: "Selected cables and routes", text: "Confirm that every risk-significant cable record includes both terminal locations and every traversed PAU before the route register is frozen.", resolved: true, resolution: "The cable-to-raceway join was rerun against the controlled routing export; all eight selected cables now include endpoints, PAUs, and principal raceways.", resolvedAt: "2026-07-10T09:20:00Z", resolvedBy: id(variant, "REVIEWER-ELECTRICAL"), severity: "MAJOR" as const },
    { uuid: id(variant, "COMMENT-02"), authorRole: "INTERNAL_REVIEWER" as const, authorId: id(variant, "REVIEWER-FIRE-PROTECTION"), createdAt: "2026-07-09T10:40:00Z", associatedSr: "FSS-C3", associatedField: "High-energy arcing scenario", text: "The high-energy arcing source needs an explicit enclosure-breach and secondary-combustible sensitivity tied to the target-damage calculation.", resolved: true, resolution: "A bounding enclosure-breach case and the 95th-percentile secondary-combustible loading were added to the fire-model sensitivity set.", resolvedAt: "2026-07-12T16:05:00Z", resolvedBy: id(variant, "REVIEWER-FIRE-PROTECTION"), severity: "MINOR" as const },
    { uuid: id(variant, "COMMENT-03"), authorRole: "INTERNAL_REVIEWER" as const, authorId: id(variant, "REVIEWER-FIRE-PROTECTION"), createdAt: "2026-07-11T08:35:00Z", associatedSr: "FIGN-A7", associatedField: "Ignition-frequency update", text: "Clarify how the pre-operational design exposure is combined with generic nuclear fire experience without implying unavailable plant operating experience.", resolved: false, severity: "MINOR" as const },
    { uuid: id(variant, "COMMENT-04"), authorRole: "INTERNAL_REVIEWER" as const, authorId: id(variant, "REVIEWER-ELECTRICAL"), createdAt: "2026-07-11T13:10:00Z", associatedSr: "FCF-A1", associatedField: "Hot-short duration probability", text: "Reconcile the credited hot-short duration factor with conductor count, energization, grounding, and protective-device clearing time for the leading control circuit.", resolved: true, resolution: "The leading circuit evaluation now cites the applicable test-data subset and separately carries the duration factor in the scenario probability.", resolvedAt: "2026-07-14T11:30:00Z", resolvedBy: id(variant, "REVIEWER-ELECTRICAL"), severity: "MAJOR" as const },
    { uuid: id(variant, "COMMENT-05"), authorRole: "INTERNAL_REVIEWER" as const, authorId: id(variant, "REVIEWER-HRA"), createdAt: "2026-07-13T09:00:00Z", associatedSr: "FHR-C1", associatedField: "Remote-shutdown timing", text: "The remote-shutdown action should show the SCBA donning time and the alternate exterior route time explicitly in the available-time margin.", resolved: false, severity: "MAJOR" as const },
    { uuid: id(variant, "COMMENT-06"), authorRole: "INTERNAL_REVIEWER" as const, authorId: id(variant, "REVIEWER-ELECTRICAL"), createdAt: "2026-07-15T15:25:00Z", associatedSr: "FESQ-B1", associatedField: "Quantification convergence", text: "Document the change in total Internal Fire frequency when truncation is reduced from 1E-12 to 1E-13 and confirm the contributor ranking remains stable.", resolved: true, resolution: "The lower-truncation run changes the aggregate result by 0.28 percent and preserves the first twelve contributor ranks.", resolvedAt: "2026-07-16T12:45:00Z", resolvedBy: id(variant, "REVIEWER-ELECTRICAL"), severity: "OBSERVATION" as const },
    { uuid: id(variant, "COMMENT-07"), authorRole: "INTERNAL_APPROVER" as const, authorId: id(variant, "APPROVER-RISK"), createdAt: "2026-07-18T14:00:00Z", associatedSr: "FHR-C1", associatedField: "Approval hold point", text: "Approval remains withheld until the remote-shutdown timing comment is resolved and independently confirmed against the final procedure route.", resolved: false, severity: "MAJOR" as const },
  ];
  mef.internalReviewComments = { comments: reviewComments, openCount: reviewComments.filter((comment) => !comment.resolved).length, resolvedCount: reviewComments.filter((comment) => comment.resolved).length };
  const evidence = [
    ["EVID-GA", "General arrangement and fire-area drawings", "DRAWING"], ["EVID-FHA", "Fire hazards analysis", "CALCULATION"], ["EVID-FPP", "Fire protection program basis", "PROCEDURE"],
    ["EVID-CABLE", "Cable and raceway database export", "DATA"], ["EVID-SLD", "Electrical single-line and control schematics", "DRAWING"], ["EVID-WALK", "Multidiscipline fire walkdown package", "WALKDOWN"],
    ["EVID-IGN", "Nuclear fire events and ignition-frequency data", "DATA"], ["EVID-FIREMODEL", "Fire modeling calculation package", "MODEL"], ["EVID-SUP", "Detection and suppression reliability study", "CALCULATION"],
    ["EVID-HRA", "Fire HRA simulator and talk-through study", "DATA"], ["EVID-PRA", "Internal-events PRA model archive", "MODEL"], ["EVID-OPS", "Safe-shutdown and abnormal fire procedures", "PROCEDURE"],
    ["EVID-OCP", "Protective-device coordination study", "CALCULATION"], ["EVID-PEER", "Internal-events peer-review disposition package", "REVIEW"], ["EVID-DATA", "Probability and uncertainty parameter notebook", "DATA"], ["EVID-CONFIG", "Design configuration freeze and turnover register", "OTHER"],
  ] as const;
  mef.evidenceRegister = evidence.map(([code, name, evidenceType], index) => ({ ...record(variant, code, name, `${name} is a controlled input to the Internal Fire PRA technical basis.`, `Revision R${String(index + 7)} was independently checked and reconciled to the 2026-06 design freeze.`), evidenceType, sourceReference: `${code}-R${String(index + 7)}`, revision: `R${String(index + 7)}`, effectiveDate: "2026-06-30", applicableSubelements: ["FPP", "FES", "FCS", "FQLS", "FPRM", "FSS", "FIGN", "FCF", "FHR", "FESQ"], applicability: `Applicable to ${context.plantName} and the modeled pre-operational configuration.`, qualityAndLimitations: index === 5 ? "All risk-significant PAUs inspected; final turnover walkdown remains scheduled." : "Configuration controlled and independently checked.", controlled: true }));
  mef.applications = [
    ["APP-RI", "Integrated risk evaluation", "Transfer Internal Fire sequence-family frequencies, uncertainty, importance, and contributors to Risk Integration."],
    ["APP-DID", "Fire defense-in-depth evaluation", "Evaluate prevention, detection, suppression, confinement, safe shutdown, and recovery layers."],
    ["APP-CONFIG", "Fire configuration risk management", "Control barriers, detection, suppression, transient combustibles, cable routing, and safe-shutdown alignments."],
  ].map(([code, name, description]) => ({ ...record(variant, code, name, description, "The application is bounded by the controlled Internal Fire model and approved risk metrics.", [id(variant, "EVID-CONFIG")]), purpose: description, decisionContext: "Licensing support, design completion, and risk-informed plant programs", supportedRiskMetrics: [context.principalMetric, "Fire scenario frequency per plant-year", "Event-sequence family frequency per plant-year"], consumingElementRefs: ["ESQ", "RI"], configurationBasis: "Design baseline 2026.06 / Internal Fire model F-R1.0", limitations: mef.metadata.limitations }));
  mef.baselinePra = { modelName: context.baselineModel, modelReference: `${variant.toUpperCase()}-PRA-MEF`, revision: variant === "htgr" ? "R6.2" : "R5.4", freezeDate: "2026-06-30", freezeStatus: "FROZEN", modelBoundary: `All defined at-power states, ${String(context.modules)} modules or units, shared systems, spent fuel, and defined release categories.`, plantOperatingStateRefs: ["POS-FULL-POWER", "POS-POWER-ASCENSION", "POS-POWER-DESCENSION"], reactorUnitRefs: Array.from({ length: context.modules }, (_, i) => `UNIT-${String(i + 1)}`), radioactiveMaterialSourceRefs: ["REACTOR-INVENTORY", "SPENT-FUEL-INVENTORY", "RADWASTE-INVENTORY"], recordTreatments: ["PLANT_OPERATING_STATES", "INITIATING_EVENTS", "EVENT_SEQUENCES", "SUCCESS_CRITERIA", "SYSTEMS", "DATA", "HUMAN_RELIABILITY", "RISK_INTEGRATION"].map((area, index) => ({ ...record(variant, `BASE-${String(index + 1).padStart(2, "0")}`, `${area.replace(/_/g, " ")} baseline treatment`, `Defines the controlled reuse or fire-specific modification of ${area.toLowerCase().replace(/_/g, " ")}.`, "A multidisciplinary interface review reconciled the baseline and fire model."), technicalArea: area as NonNullable<InternalFirePRA["baselinePra"]>["recordTreatments"][number]["technicalArea"], sourceRecordRefs: [`BASELINE-${area}`], treatment: index < 2 ? "REUSED" : "MODIFIED", internalFireChange: index < 2 ? "Baseline definition retained with fire-specific applicability confirmed." : "Spatial fire damage, spurious operations, fire context, and dependencies added.", unresolvedItems: [] })), unresolvedInterfaces: [] };

  const pauIds = context.buildings.map((_, i) => id(variant, `FPP-PAU-${String(i + 1).padStart(2, "0")}`));
  const barrierIds = context.buildings.map((_, i) => id(variant, `FPP-BAR-${String(i + 1).padStart(2, "0")}`));
  const globalBoundary = { ...record(variant, "FPP-BOUNDARY-001", `${context.plantName} global fire analysis boundary`, "Includes every licensee-controlled location where a fire can affect modeled equipment, cables, shared support, or a radioactive-material source.", "The boundary follows controlled fire-area and general-arrangement drawings and explicitly addresses shared and multi-unit locations.", [id(variant, "EVID-GA"), id(variant, "EVID-WALK")]), includedLocationRefs: context.buildings.map((item) => item.toUpperCase().replace(/\s+/g, "-")), excludedLocations: [{ uuid: id(variant, "FPP-EXCL-001"), location: "Administration annex", exclusionBasis: "No modeled equipment, cables, fire propagation path, or radioactive material source is present.", evidenceRefs: [id(variant, "EVID-GA"), id(variant, "EVID-WALK")] }], licenseeControlledAreaDescription: `All safety-related and risk-significant structures within the ${context.site} protected area.`, reactorUnitRefs: mef.baselinePra.reactorUnitRefs, radioactiveMaterialSourceRefs: mef.baselinePra.radioactiveMaterialSourceRefs, atPowerOperatingStateRefs: mef.baselinePra.plantOperatingStateRefs, multiUnitOrMultiSourceLocations: ["Main control room", "Essential electrical galleries", "Shared radwaste transfer corridor", "Spent-fuel handling area"] };
  const paus = context.buildings.map((building, index) => ({ ...record(variant, `FPP-PAU-${String(index + 1).padStart(2, "0")}`, `${building} PAU ${String(index + 1).padStart(2, "0")}`, `Fire compartment representing the principal risk-significant volume in ${building}.`, "The PAU boundary follows rated construction, spatial separation, ventilation, fire loading, equipment distribution, and credible fire-damage extent.", [id(variant, "EVID-GA"), id(variant, "EVID-FHA"), id(variant, "EVID-WALK")]), fireArea: `FA-${String(index + 1).padStart(2, "0")}`, fireCompartment: `FC-${String(index + 1).padStart(2, "0")}`, building, rooms: [`${building.slice(0, 3).toUpperCase()}-${String(110 + index)}`, `${building.slice(0, 3).toUpperCase()}-${String(210 + index)}`], elevation: `${String(100 - (index % 4) * 4.6)} m plant datum`, volumeCubicMetres: 680 + index * 135, boundaryElementRefs: [barrierIds[index]!], adjacentPauRefs: [pauIds[(index + 1) % pauIds.length]!], ventilationZones: [`HVAC-Z-${String(index + 1).padStart(2, "0")}`], reactorUnitRefs: [mef.baselinePra!.reactorUnitRefs[index % context.modules] ?? "UNIT-1"], radioactiveMaterialSourceRefs: [index === 6 ? "SPENT-FUEL-INVENTORY" : "REACTOR-INVENTORY"], fixedIgnitionSourceRefs: [id(variant, `FSS-IGN-${String(index + 1).padStart(2, "0")}`)], transientCombustibleZones: [`TCZ-${String(index + 1).padStart(2, "0")}`], creditedBarrierRefs: [barrierIds[index]!], fireProtectionFeatures: ["Addressable smoke detection", index % 2 === 0 ? "Automatic water suppression" : "Automatic clean-agent suppression", "Portable extinguishers", "Pre-fire plan"] }));
  const barriers = context.buildings.map((building, index) => ({ ...record(variant, `FPP-BAR-${String(index + 1).padStart(2, "0")}`, `${building} credited fire barrier`, `Separates ${paus[index]!.name} from ${paus[(index + 1) % paus.length]!.name}.`, "Barrier rating, penetrations, doors, and surveillance controls were reconciled to the fire protection program.", [id(variant, "EVID-GA"), id(variant, "EVID-FPP")]), elementType: index % 3 === 0 ? "RATED_DOOR" as const : "RATED_WALL" as const, fromPauRef: pauIds[index]!, toPauRef: pauIds[(index + 1) % pauIds.length]!, fireResistanceRatingMinutes: index % 3 === 0 ? 90 : 180, credited: true, maintainedByFireProtectionProgram: true, condition: "Acceptable; penetrations and closures included in impairment control.", containmentBasis: "Qualification record and surveillance demonstrate the credited fire-resistance function." }));
  const coverage = { ...record(variant, "FPP-COVER-001", "Complete and nonoverlapping PAU reconciliation", "Assigns every in-scope room, bay, shaft, tunnel, and outdoor equipment location to exactly one PAU.", "Automated room-list reconciliation plus independent fire-drawing review identified no gap or overlap.", [id(variant, "EVID-GA")]), complete: true, nonOverlapping: true, unassignedLocations: [], overlappingPauPairs: [], excludedLocationRefs: [id(variant, "FPP-EXCL-001")], reconciliationMethod: "Room database join, fire-area topology check, ventilation-zone cross-check, and independent 100 percent review." };
  const ppInvestigation = { ...record(variant, "FPP-INV-001", "Fire partitioning and spatial-feature walkdown", "Confirmed PAU boundaries, fire doors, penetrations, ventilation interfaces, ignition sources, targets, and fire-protection features.", "Every risk-significant PAU and all credited barriers were inspected by a multidisciplinary team.", [id(variant, "EVID-WALK")]), investigationType: "WALKDOWN" as const, scope: "All modeled PAUs and credited fire-barrier interfaces", plantOperatingStateRefs: mef.baselinePra.plantOperatingStateRefs, locations: context.buildings, participants: ["Fire PRA lead", "Fire protection engineer", "Electrical engineer", "Systems engineer", "Operations representative"], performedDate: "2026-05-19", observations: ["Two transient-combustible zones relocated outside cable concentration areas.", "One fire-door surveillance reference corrected.", "Ventilation boundary identifiers reconciled to the current drawing revision."], findingRefs: ["FW-017", "FW-024"], confirmedRecordRefs: [...pauIds, ...barrierIds] };
  const ppUncertainty = uncertainty(variant, "FPP", pauIds); const ppPreop = preop(variant, "FPP", barrierIds);
  mef.plantBoundaryAndPartitioning = { globalBoundary, physicalAnalysisUnits: paus, partitioningElements: barriers, coverageChecks: [coverage], investigations: [ppInvestigation], modelUncertainties: [ppUncertainty], preOperationalAssumptions: [ppPreop], documentation: documentation(variant, "FPP", "Internal Fire Plant Boundary and Partitioning documentation", "Define the global boundary, divide it into complete nonoverlapping PAUs, characterize every partition, and confirm credited features.", "General arrangements, fire-area drawings, ventilation zones, barrier schedules, source and target databases, and walkdown observations.", "Spatial topology, fire compartmentation, ventilation and fire-hazard review, room-list reconciliation, and multidisciplinary walkdown.", `${String(paus.length)} PAUs cover all in-scope locations without overlap; every exclusion and credited barrier is traceable.`) };
  cover([globalBoundary, ...paus, ...barriers, coverage, ppInvestigation, ppUncertainty, ppPreop], "FPP");

  const equipment = context.equipment.map((equipmentRef, index) => ({ ...record(variant, `FES-EQ-${String(index + 1).padStart(2, "0")}`, `${equipmentRef} fire equipment selection`, `${equipmentRef} is selected for its role in ${context.systems[index]}.`, "Systematic initiating-event, success-criteria, safe-shutdown, containment, multi-unit, and human-action support reviews established the selection.", [id(variant, "EVID-PRA"), id(variant, "EVID-WALK")]), equipmentRef, systemRef: context.systems[index]!.toUpperCase().replace(/\s+/g, "-"), equipmentType: index % 3 === 0 ? "Protection cabinet" : index % 3 === 1 ? "Motor-operated component" : "Electrical distribution component", creditedFunctions: [`Maintain ${context.systems[index]} function`, "Support controlled plant response"], selectionBasis: (index < 2 ? ["INITIATING_EVENT", "SAFE_SHUTDOWN"] : index === 6 ? ["HUMAN_ACTION_SUPPORT", "CONTAINMENT"] : ["MITIGATION", "SAFE_SHUTDOWN"]) as InternalFirePRA["equipmentSelection"]["equipmentSelections"][number]["selectionBasis"], fireFailureModes: (index % 2 === 0 ? ["LOSS_OF_FUNCTION", "SPURIOUS_OPERATION"] : ["LOSS_OF_FUNCTION", "LOSS_OF_SIGNAL"]) as InternalFirePRA["equipmentSelection"]["equipmentSelections"][number]["fireFailureModes"], affectedInitiatingEventRefs: [`IE-FIRE-${String((index % 3) + 1)}`], affectedSuccessCriterionRefs: [`SC-FIRE-${String((index % 4) + 1)}`], physicalAnalysisUnitRefs: [pauIds[index]!], cableRefs: [id(variant, `FCS-CBL-${String(index + 1).padStart(2, "0")}`)], modelBasicEventRefs: [`BE-FIRE-${equipmentRef}`], disposition: "INCLUDE" as const }));
  const initiatorSelections = ["Loss of essential power", "Loss of shutdown heat removal", "Spurious protection actuation", "Loss of control and indication"].map((name, index) => ({ ...record(variant, `FES-IE-${String(index + 1).padStart(2, "0")}`, `${name} selection`, `Fire-induced ${name.toLowerCase()} is retained for plant-response modeling.`, "Internal-events initiators and credible individual and combined spurious operations were systematically reviewed.", [id(variant, "EVID-PRA"), id(variant, "EVID-SLD")]), initiatingEventRef: `IE-FIRE-${String(index + 1)}`, sourceTechnicalElementRef: `IE-BASE-${String(index + 1)}`, selectionType: index < 2 ? "BASELINE_INCLUDED" as const : "FIRE_UNIQUE" as const, fireCausingEquipmentRefs: equipment.slice(index, index + 2).map((item) => item.uuid), spuriousOperationCombinations: index > 1 ? [[equipment[index]!.uuid, equipment[(index + 1) % equipment.length]!.uuid]] : [], affectedReactorUnitRefs: mef.baselinePra!.reactorUnitRefs, affectedRadioactiveMaterialSourceRefs: ["REACTOR-INVENTORY"], disposition: "INCLUDE" as const }));
  const instruments = equipment.slice(0, 4).map((item, index) => ({ ...record(variant, `FES-INS-${String(index + 1).padStart(2, "0")}`, `${item.equipmentRef} operator indication`, "Selected instrumentation provides a credited cue or can induce an undesirable response under fire conditions.", "Procedure and HRA reviews identify all indications required by modeled actions.", [id(variant, "EVID-SLD"), id(variant, "EVID-HRA")]), instrumentRef: `INS-${item.equipmentRef}`, monitoredParameter: ["Reactor trip status", "Decay heat removal flow", "Essential bus voltage", "Fire area temperature"][index]!, supportedHumanActionRefs: [id(variant, `FHR-ACT-${String(index + 1).padStart(2, "0")}`)], credibleFailureModes: (index % 2 === 0 ? ["LOSS_OF_SIGNAL", "SPURIOUS_INDICATION"] : ["ERRONEOUS_INDICATION"]) as InternalFirePRA["equipmentSelection"]["instrumentationSelections"][number]["credibleFailureModes"], undesirableOperatorResponse: "Delay or misdirect safe-shutdown alignment until diverse indication is checked.", physicalAnalysisUnitRefs: [pauIds[index]!], cableRefs: [id(variant, `FCS-CBL-${String(index + 1).padStart(2, "0")}`)], disposition: "INCLUDE" as const }));
  const fesUncertainty = uncertainty(variant, "FES", equipment.map((item) => item.uuid)); const fesPreop = preop(variant, "FES", instruments.map((item) => item.uuid));
  mef.equipmentSelection = { initiatingEventSelections: initiatorSelections, equipmentSelections: equipment, instrumentationSelections: instruments, modelUncertainties: [fesUncertainty], preOperationalAssumptions: [fesPreop], documentation: documentation(variant, "FES", "Internal Fire Equipment Selection documentation", "Select equipment and instrumentation capable of causing initiators, defeating mitigation or safe shutdown, affecting containment, or changing operator response.", "Internal-events logic, success criteria, system schematics, safe-shutdown analysis, fire drawings, and operator procedures.", "Top-down PRA logic review, bottom-up equipment and spurious-operation search, instrumentation review, and walkdown confirmation.", `${String(equipment.length)} equipment items, ${String(instruments.length)} indications, and ${String(initiatorSelections.length)} initiating-event treatments are retained.`) };
  cover([...initiatorSelections, ...equipment, ...instruments, fesUncertainty, fesPreop], "FES");

  const cableIds = equipment.map((_, i) => id(variant, `FCS-CBL-${String(i + 1).padStart(2, "0")}`));
  const racewayIds = equipment.map((_, i) => id(variant, `FCS-RWY-${String(i + 1).padStart(2, "0")}`));
  const cables = equipment.map((item, index) => ({ ...record(variant, `FCS-CBL-${String(index + 1).padStart(2, "0")}`, `${item.equipmentRef} selected cable`, `Selected ${index % 3 === 0 ? "control" : index % 3 === 1 ? "power" : "instrumentation"} cable supporting ${item.creditedFunctions[0]}.`, "Cable endpoints and routing were traced from current schematics through the controlled cable and raceway database.", [id(variant, "EVID-CABLE"), id(variant, "EVID-SLD")]), cableId: `CBL-${String(index + 1).padStart(3, "0")}`, equipmentRef: item.uuid, function: item.creditedFunctions[0]!, cableType: index % 3 === 0 ? "CONTROL" as const : index % 3 === 1 ? "POWER" as const : "INSTRUMENTATION" as const, voltageClass: index % 3 === 1 ? "4.16 kV" : "125 V", fromTermination: `TERM-SOURCE-${String(index + 1)}`, toTermination: `TERM-${item.equipmentRef}`, physicalAnalysisUnitRefs: [pauIds[index]!, pauIds[(index + 1) % pauIds.length]!], racewayRefs: [racewayIds[index]!], associatedFailureModes: ["Open circuit", index % 2 === 0 ? "Conductor-to-conductor hot short" : "Short to ground"], modelBasicEventRefs: item.modelBasicEventRefs, routingStatus: index === 7 ? "ASSUMED" as const : "EXACT" as const, routingBasis: index === 7 ? "Bounding route through all credible exposed PAUs pending final as-built pull-card turnover." : "Exact route from the controlled cable and raceway database.", riskSignificant: true }));
  const raceways = equipment.map((item, index) => ({ ...record(variant, `FCS-RWY-${String(index + 1).padStart(2, "0")}`, `${item.equipmentRef} principal raceway`, `Principal raceway segment carrying the selected cable for ${item.equipmentRef}.`, "Raceway identification, PAU, elevation, fire protection, and material were reconciled to drawings and walkdown.", [id(variant, "EVID-CABLE"), id(variant, "EVID-WALK")]), racewayId: `RWY-${String(index + 1).padStart(3, "0")}`, racewayType: index % 3 === 0 ? "CABLE_TRAY" as const : index % 3 === 1 ? "CONDUIT" as const : "PENETRATION" as const, physicalAnalysisUnitRef: pauIds[index]!, routeDescription: `From electrical source through ${paus[index]!.fireCompartment} to ${item.equipmentRef}.`, elevation: paus[index]!.elevation, fireBarrierProtectionRefs: [barrierIds[index]!], cableRefs: [cableIds[index]!], materialAndConfiguration: index % 2 === 0 ? "Galvanized ladder tray, 450 mm wide, stacked configuration" : "Rigid steel conduit with qualified penetration seal", sourceDocumentRefs: [id(variant, "EVID-CABLE"), id(variant, "EVID-GA")] }));
  const assumedRouting = [{ ...record(variant, "FCS-ASSUME-001", "Spent-fuel cooling control-cable bounding route", "Bounds the unconfirmed final route through every credible exposed PAU between the control and fuel-handling buildings.", "The assumed route is deliberately conservative and cannot omit a risk-significant exposure.", [id(variant, "EVID-CONFIG")]), cableRefs: [cableIds[7]!], assumedPauRefs: [pauIds[3]!, pauIds[6]!, pauIds[7]!], assumedRacewayRefs: [racewayIds[7]!], scopeAndExtent: "Control and indication conductors from the main control room to the spent-fuel cooling starter.", conservatism: "Assumes simultaneous damage in each plausible shared raceway segment.", closureAction: "Reconcile final pull cards and raceway walkdown before fuel-load readiness." }];
  const ocp = equipment.slice(0, 4).map((item, index) => ({ ...record(variant, `FCS-OCP-${String(index + 1).padStart(2, "0")}`, `${item.equipmentRef} overcurrent protection assessment`, "Evaluates fire-induced faults and protective-device coordination for the selected circuit.", "Coordination study demonstrates isolation without upstream loss beyond the modeled dependency boundary.", [id(variant, "EVID-OCP")]), distributionBusRef: `BUS-${String(index + 1)}`, protectiveDeviceRefs: [`BRKR-${String(index + 1)}A`, `FUSE-${String(index + 1)}B`], coordinationDocumentRef: `OCP-CALC-${String(index + 1).padStart(3, "0")}`, coordinationAdequate: true, additionalCircuitRefs: index === 2 ? [cableIds[4]!] : [], challengeDescription: "Fire-induced short-circuit current and clearing sequence across series protection.", modelTreatment: "No upstream propagation; local circuit failure retained." }));
  const fcsUncertainty = uncertainty(variant, "FCS", cableIds); const fcsPreop = preop(variant, "FCS", [cableIds[7]!]);
  mef.cableSelectionAndLocation = { raceways, cables, assumedRouting, overcurrentProtectionAssessments: ocp, modelUncertainties: [fcsUncertainty], preOperationalAssumptions: [fcsPreop], documentation: documentation(variant, "FCS", "Internal Fire Cable Selection and Location documentation", "Select and route cables supporting modeled equipment and evaluate raceways, assumed routes, and overcurrent protection.", "Cable database, routing drawings, pull cards, single-line diagrams, control schematics, fire areas, and coordination studies.", "Endpoint-to-endpoint cable tracing, PAU/raceway spatial join, independent sample verification, bounding-route control, and protective-device review.", `${String(cables.length)} risk-significant cables and ${String(raceways.length)} principal raceways are traced; one conservative assumed route remains controlled.`) };
  cover([...raceways, ...cables, ...assumedRouting, ...ocp, fcsUncertainty, fcsPreop], "FCS");

  const screeningCriteria = [
    { ...record(variant, "FQLS-CRIT-01", "No fire-induced initiating event or mitigation impact", "Screens a PAU only when no credible fire can cause an initiating event or damage equipment, cables, or indications needed by the PRA.", "Criterion implements the approved qualitative screen with complete equipment and cable-location evidence.", [id(variant, "EVID-FHA"), id(variant, "EVID-CABLE")]) },
    { ...record(variant, "FQLS-CRIT-02", "No propagation to a retained PAU", "Screens a PAU only when its fire cannot propagate through ventilation, openings, penetrations, or barrier failure to a retained location.", "Rated construction and propagation paths are verified against controlled drawings and walkdown.", [id(variant, "EVID-GA"), id(variant, "EVID-WALK")]) },
    { ...record(variant, "FQLS-CRIT-03", "Retain for detailed quantification", "Retains a PAU whenever either qualitative criterion is not conclusively demonstrated.", "Retention avoids unsupported qualitative credit and preserves conservative completeness.", [id(variant, "EVID-FHA")]) },
  ];
  const screeningDecisions = paus.map((pau, index) => ({ ...record(variant, `FQLS-DEC-${String(index + 1).padStart(2, "0")}`, `${pau.name} qualitative disposition`, index < 5 ? "Retained because modeled equipment, cables, or operator cues are exposed to credible fire." : "Screened because no modeled target exists and propagation to retained targets is prevented by verified rated construction.", "The approved criteria were applied consistently using the equipment, cable, ignition-source, barrier, and walkdown databases.", [id(variant, "EVID-FHA"), id(variant, "EVID-WALK")]), screenedObjectType: "PAU" as const, screenedObjectRefs: [pau.uuid], criterion: index < 5 ? "RETAINED" as const : index % 2 === 0 ? "FQLS-A1" as const : "FQLS-A2" as const, disposition: index < 5 ? "RETAINED" as const : "SCREENED" as const, conservativeAssumptions: index < 5 ? ["All selected cables in the PAU are exposed unless protected by a qualified barrier."] : ["Transient combustible loading at the administrative limit.", "No suppression credit is required for screening."], affectedEventSequenceFamilyRefs: index < 5 ? [`ESF-FIRE-${String((index % 4) + 1)}`] : [] }));
  const fqlsUncertainty = uncertainty(variant, "FQLS", screeningDecisions.map((item) => item.uuid)); const fqlsPreop = preop(variant, "FQLS", screeningDecisions.slice(5).map((item) => item.uuid));
  mef.qualitativeScreening = { screeningCriteria, screeningDecisions, retainedPauRefs: pauIds.slice(0, 5), screenedPauRefs: pauIds.slice(5), modelUncertainties: [fqlsUncertainty], preOperationalAssumptions: [fqlsPreop], documentation: documentation(variant, "FQLS", "Internal Fire Qualitative Screening documentation", "Apply approved qualitative criteria to every PAU and retain any location that cannot be conclusively screened.", "PAU, equipment, cable, ignition-source, barrier, propagation, and walkdown registers.", "Two-condition qualitative screen with conservative assumptions, independent review, and complete PAU reconciliation.", `Five PAUs are retained for detailed scenario development and three are screened with traceable criteria.`) };
  cover([...screeningCriteria, ...screeningDecisions, fqlsUncertainty, fqlsPreop], "FQLS");

  const peerReview = ["SY-A5-01", "ES-B2-03", "HRA-C1-02"].map((findingRef, index) => ({ ...record(variant, `FPRM-PR-${String(index + 1).padStart(2, "0")}`, `${findingRef} fire applicability disposition`, "Evaluates whether the internal-events peer-review finding affects fire plant-response logic or data.", "The fire model inherits the approved resolution or adds an explicit fire-specific treatment.", [id(variant, "EVID-PEER")]), sourcePeerReviewRef: "INTERNAL-EVENTS-PEER-REVIEW-2025", findingRef, applicability: index === 0 ? "Directly applicable to system logic used in fire scenarios." : "Applicable to the corresponding event-sequence or HRA treatment.", disposition: "Resolved in the fire model baseline and independently verified.", incorporated: true, verificationRefs: [`VERIFY-${findingRef}`] }));
  const initiatingModels = initiatorSelections.slice(0, 3).map((selection, index) => ({ ...record(variant, `FPRM-IE-${String(index + 1).padStart(2, "0")}`, `${selection.name} plant-response model`, "Connects selected fire initiators and affected units to the corresponding event tree and scenario set.", "The model retains the internal-events structure where valid and introduces fire-specific branches where spatial damage changes response.", [id(variant, "EVID-PRA")]), initiatingEventRef: selection.initiatingEventRef, sourceSelectionRef: selection.uuid, eventTreeRef: `ET-FIRE-${String(index + 1)}`, fireScenarioRefs: [id(variant, `FSS-SCEN-${String(index + 1).padStart(2, "0")}`), id(variant, `FSS-SCEN-${String(index + 4).padStart(2, "0")}`)], affectedUnitRefs: mef.baselinePra!.reactorUnitRefs, plantResponseModelTreatment: index === 2 ? "NEW" as const : "MODIFIED" as const }));
  const eventSequences = ["Essential power recovery", "Decay heat removal success", "Alternate shutdown", "Control-room abandonment"].map((name, index) => ({ ...record(variant, `FPRM-ES-${String(index + 1).padStart(2, "0")}`, `${name} event-sequence model`, `Models ${name.toLowerCase()} under fire-induced equipment, circuit, and human-response conditions.`, "Top events and end states were reconciled to the baseline PRA and fire safe-shutdown procedures.", [id(variant, "EVID-PRA"), id(variant, "EVID-OPS")]), initiatingEventRef: initiatorSelections[index % initiatorSelections.length]!.initiatingEventRef, baselineEventSequenceRef: `ES-BASE-${String(index + 1)}`, fireScenarioRefs: [id(variant, `FSS-SCEN-${String(index + 1).padStart(2, "0")}`)], topEvents: ["Automatic protection succeeds", "Credited train available", "Required fire action succeeds"], endStates: index === 3 ? ["CONTROL_ROOM_ABANDONMENT", "SAFE_SHUTDOWN", "RELEASE_CATEGORY_2"] : ["SAFE_SHUTDOWN", `RELEASE_CATEGORY_${String((index % 3) + 1)}`], fireProcedureRefs: ["AOP-FIRE-001", `FSP-${String(index + 1).padStart(3, "0")}`], eventSequenceFamilyRefs: [`ESF-FIRE-${String(index + 1)}`], modelTreatment: index === 3 ? "NEW" as const : "MODIFIED" as const }));
  const successCriteria = ["Reactor shutdown", "Decay heat removal", "Inventory confinement", "Spent-fuel cooling"].map((fn, index) => ({ ...record(variant, `FPRM-SC-${String(index + 1).padStart(2, "0")}`, `${fn} fire success criterion`, `Defines success for ${fn.toLowerCase()} after a fire-induced initiator.`, "Thermal-hydraulic, system-capacity, mission-time, and fire-specific boundary conditions support the criterion.", [id(variant, "EVID-PRA")]), function: fn, baselineSuccessCriterionRef: `SC-BASE-${String(index + 1)}`, successDefinition: index === 0 ? "Insert sufficient negative reactivity and prevent recriticality." : `Maintain credited ${fn.toLowerCase()} capacity throughout the modeled mission.`, missionTimeHours: index === 0 ? 0.25 : index === 3 ? 72 : 24, requiredSystemTrainRefs: [context.systems[index % context.systems.length]!.toUpperCase().replace(/\s+/g, "-")], fireSpecificConditions: ["Assume fire-damaged train unavailable.", "Credit only spatially separated or qualified alternate equipment."], analysisRef: `SC-CALC-FIRE-${String(index + 1)}`, modelTreatment: "MODIFIED" as const }));
  const systemMods = equipment.map((item, index) => ({ ...record(variant, `FPRM-SYS-${String(index + 1).padStart(2, "0")}`, `${item.systemRef} fire model modification`, `Introduces spatially correlated fire damage, circuit effects, and required HFE logic for ${item.systemRef}.`, "The modification traces selected equipment and cables to existing basic events and new fire split fractions.", [id(variant, "EVID-PRA"), id(variant, "EVID-CABLE")]), systemRef: item.systemRef, baselineModelRef: `SY-BASE-${String(index + 1)}`, affectedBasicEventRefs: item.modelBasicEventRefs, fireEquipmentRefs: [item.uuid], cableRefs: item.cableRefs, spuriousOperationRefs: index % 2 === 0 ? [id(variant, `FCF-EVAL-${String(index + 1).padStart(2, "0")}`)] : [], humanFailureEventRefs: [id(variant, `FHR-HFE-${String((index % 6) + 1).padStart(2, "0")}`)], splitFractionRefs: [`SF-FIRE-${String(index + 1)}`], modelChange: "Adds scenario-conditioned loss-of-function and spurious-operation basic events while preserving nonfire random-failure logic." }));
  const parameters = ["Diesel start failure", "DC battery unavailability", "HVAC fan recovery", "Fire door failure", "Suppression demand failure"].map((name, index) => ({ ...record(variant, `FPRM-DATA-${String(index + 1).padStart(2, "0")}`, `${name} fire-context parameter`, `Controls the ${name.toLowerCase()} probability used in fire plant-response quantification.`, "The baseline value was reviewed for fire context, common cause, environmental qualification, and dependency.", [id(variant, "EVID-DATA")]), parameterRef: `DA-FIRE-${String(index + 1)}`, parameterType: index === 3 ? "FIRE_CONTEXT_OTHER" as const : index === 4 ? "UNAVAILABILITY" as const : "RANDOM_FAILURE" as const, baselineValue: 0.01 + index * 0.005, fireContextValue: 0.015 + index * 0.006, uncertaintyDistribution: "Lognormal, error factor 3", sourceDataRef: id(variant, "EVID-DATA"), reanalysisRequired: index > 2 }));
  const fprmUncertainty = uncertainty(variant, "FPRM", systemMods.map((item) => item.uuid)); const fprmPreop = preop(variant, "FPRM", parameters.map((item) => item.uuid));
  mef.plantResponseModel = { peerReviewDispositions: peerReview, initiatingEventModels: initiatingModels, eventSequenceModels: eventSequences, successCriteria, systemModelModifications: systemMods, probabilityDataParameters: parameters, modelUncertainties: [fprmUncertainty], preOperationalAssumptions: [fprmPreop], documentation: documentation(variant, "FPRM", "Internal Fire Plant Response Model documentation", "Adapt initiating events, event sequences, success criteria, systems logic, HFEs, and data to spatially correlated fire conditions.", "Baseline PRA, peer-review dispositions, selected equipment and cables, circuits, procedures, success-criteria analyses, and probability data.", "Model reuse assessment, fire-specific logic modification, scenario conditioning, spurious-operation review, and independent model comparison.", `Four fire event-sequence models and eight system modifications connect retained scenarios to controlled end states.`) };
  cover([...peerReview, ...initiatingModels, ...eventSequences, ...successCriteria, ...systemMods, ...parameters, fprmUncertainty, fprmPreop], "FPRM");

  const frequencyGroups = ["Electrical cabinets", "Switchgear", "Pumps and motors", "Transformers", "Transient combustibles", "High-energy arcing"].map((category, index) => ({ ...record(variant, `FIGN-GRP-${String(index + 1).padStart(2, "0")}`, `${category} ignition-frequency group`, `Plant-wide ignition-frequency group for ${category.toLowerCase()}.`, "Generic nuclear experience is screened for technology applicability and updated using plant design and exposure information.", [id(variant, "EVID-IGN")]), sourceCategory: category, eventDataSourceRefs: [id(variant, `FIGN-DATA-${String((index % 4) + 1).padStart(2, "0")}`)], genericFrequencyPerPlantYear: [0.022, 0.0068, 0.014, 0.0031, 0.021, 0.0014][index]!, updatedFrequencyPerPlantYear: [0.019, 0.0061, 0.012, 0.0028, 0.017, 0.0013][index]!, updateMethod: index < 4 ? "BAYESIAN" as const : "EQUIVALENT_STATISTICAL" as const, plantAvailabilityFactor: index === 4 ? 0.75 : 0.96, uncertaintyDistribution: "Gamma-Poisson posterior with lognormal apportionment uncertainty", parameterDependencies: ["Plant operating year exposure", "Source population", "Energetic duty factor"] }));
  const ignitionSources = paus.map((pau, index) => ({ ...record(variant, `FSS-IGN-${String(index + 1).padStart(2, "0")}`, `${context.equipment[index]} ignition source`, `${context.equipment[index]} and its local combustible loading define a credible ignition source in ${pau.name}.`, "Source type, location, fuel, frequency category, heat-release profile, and secondary combustibles were confirmed by walkdown.", [id(variant, "EVID-FHA"), id(variant, "EVID-WALK")]), physicalAnalysisUnitRef: pau.uuid, sourceType: index === 3 ? "SWITCHGEAR" as const : index === 5 ? "MOTOR" as const : index === 7 ? "TRANSIENT_COMBUSTIBLE" as const : "ELECTRICAL_CABINET" as const, mobility: index === 7 ? "TRANSIENT" as const : "FIXED" as const, locationDescription: `${pau.rooms[0]} near the principal selected cable concentration`, fuelDescription: index === 7 ? "Administratively limited polymeric maintenance materials, 55 MJ/m²" : "Internal cable insulation, lubricants, filters, and adjacent qualified cable jackets", ignitionFrequencyGroupRef: frequencyGroups[index % frequencyGroups.length]!.uuid, heatReleaseRateProfileRef: `HRR-PROFILE-${String(index + 1).padStart(2, "0")}`, secondaryCombustibleRefs: [`SEC-COMB-${String(index + 1).padStart(2, "0")}`], highHazard: index === 3 || index === 7, structuralSteelExposureRefs: index === 3 ? ["STEEL-COL-CB-04"] : [] }));
  const scenarioCount = 6;
  const targetSets = Array.from({ length: scenarioCount }, (_, index) => ({ ...record(variant, `FSS-TGT-${String(index + 1).padStart(2, "0")}`, `Scenario ${String(index + 1)} damage target set`, "Groups all selected equipment, cables, and raceways exposed by the scenario fire plume, hot-gas layer, flame, smoke, or structural effect.", "Targets are identified through three-dimensional walkdown, cable routing, fire-model geometry, and conservative secondary-combustible treatment.", [id(variant, "EVID-CABLE"), id(variant, "EVID-WALK")]), physicalAnalysisUnitRefs: index === 4 ? [pauIds[index]!, pauIds[(index + 1) % pauIds.length]!] : [pauIds[index]!], equipmentRefs: [equipment[index]!.uuid, equipment[(index + 1) % equipment.length]!.uuid], cableRefs: [cableIds[index]!, cableIds[(index + 1) % cableIds.length]!], racewayRefs: [racewayIds[index]!, racewayIds[(index + 1) % racewayIds.length]!], failureModes: ["Loss of function", index % 2 === 0 ? "Spurious operation" : "Spurious indication"], damageMechanisms: (index === 4 ? ["THERMAL", "HOT_GAS_LAYER", "SMOKE"] : ["THERMAL", "FLAME_IMPINGEMENT"]) as InternalFirePRA["scenarioSelectionAndAnalysis"]["damageTargetSets"][number]["damageMechanisms"], damageThresholds: [{ uuid: id(variant, `FSS-THR-${String(index + 1)}`), targetRef: cableIds[index]!, parameter: "Thermoset cable surface temperature", value: 205, unit: "°C", sourceRef: id(variant, "EVID-FIREMODEL") }], modelBasicEventRefs: [`BE-DAMAGE-${String(index + 1)}`] }));
  const fireModels = Array.from({ length: scenarioCount }, (_, index) => ({ ...record(variant, `FSS-MODEL-${String(index + 1).padStart(2, "0")}`, `Scenario ${String(index + 1)} fire model`, "Calculates fire growth, plume and hot-gas-layer exposure, target damage time, severity, and conditional damage probability.", "The selected model is within validated compartment geometry, ventilation, fuel, and target-distance limits.", [id(variant, "EVID-FIREMODEL")]), fireScenarioRef: id(variant, `FSS-SCEN-${String(index + 1).padStart(2, "0")}`), toolName: index === 4 ? "CFAST" : "FDTs", toolVersion: index === 4 ? "7.7.5" : "1.3", modelType: index === 4 ? "COMPUTATIONAL" as const : "ANALYTICAL" as const, applicabilityLimits: ["Compartment geometry within validation range", "Well-ventilated hydrocarbon or polymeric fire", "Target distance exceeds flame-zone minimum"], inputValues: [{ uuid: id(variant, `FSS-IN-${String(index + 1)}`), parameter: "Ceiling height", value: 4.2 + (index % 3) * 0.6, unit: "m", sourceRef: id(variant, "EVID-GA") }, { uuid: id(variant, `FSS-HRR-${String(index + 1)}`), parameter: "Peak heat release rate", value: 650 + index * 180, unit: "kW", sourceRef: id(variant, "EVID-FIREMODEL") }], peakHeatReleaseRateKw: 650 + index * 180, growthTimeMinutes: 6 + index, steadyBurnMinutes: 22 + index * 3, decayTimeMinutes: 12, targetDamageTimesMinutes: [{ uuid: id(variant, `FSS-DMG-${String(index + 1)}`), targetRef: cableIds[index]!, damageTimeMinutes: 14 + index * 2, mechanism: "Thermal cable damage" }], severityFactor: 0.38 + index * 0.07, conditionalTargetDamageProbability: 0.31 + index * 0.08, uncertaintyTreatment: "Latin-hypercube propagation of HRR, growth, ventilation, target distance, and damage threshold.", withinApplicabilityLimits: true }));
  const suppression = Array.from({ length: scenarioCount }, (_, index) => ({ ...record(variant, `FSS-SUP-${String(index + 1).padStart(2, "0")}`, `Scenario ${String(index + 1)} detection and suppression`, "Evaluates automatic detection, fixed suppression, brigade response, damage timing, and conditional nonsuppression.", "Detection and suppression credit follows qualified system design, surveillance, response timelines, and dependency assessment.", [id(variant, "EVID-SUP"), id(variant, "EVID-FPP")]), physicalAnalysisUnitRef: pauIds[index]!, fireScenarioRef: id(variant, `FSS-SCEN-${String(index + 1).padStart(2, "0")}`), detectionSystemRefs: [`DET-Z-${String(index + 1)}`], automaticSuppressionSystemRefs: index % 2 === 0 ? [`SPR-Z-${String(index + 1)}`] : [`CLEAN-Z-${String(index + 1)}`], manualSuppressionCredited: index !== 5, timeToDetectionMinutes: 1.4 + index * 0.25, timeAvailableBeforeDamageMinutes: 14 + index * 2, nonsuppressionProbability: 0.08 + index * 0.025, fifthPercentile: 0.025, ninetyFifthPercentile: 0.28, dependencies: ["Common power to detection and annunciation", "Fire brigade access through adjacent PAU"], effectivenessBasis: "System reliability, surveillance, detector response, suppression activation, brigade travel, and target-damage timing are explicitly combined." }));
  const barrierAssessments = barriers.slice(0, 4).map((barrier, index) => ({ ...record(variant, `FSS-BAR-${String(index + 1).padStart(2, "0")}`, `${barrier.name} scenario assessment`, "Quantifies the reliability and availability of the credited barrier under scenario-specific fire conditions.", "Rating, penetrations, door closure, impairment controls, and qualification records support the credited effectiveness.", [id(variant, "EVID-FPP")]), barrierRef: barrier.uuid, affectedPauRefs: [barrier.fromPauRef, barrier.toPauRef!], fireResistanceRatingMinutes: barrier.fireResistanceRatingMinutes, rated: true, active: barrier.elementType === "RATED_DOOR", reliability: 0.992 - index * 0.002, availability: 0.997 - index * 0.001, qualificationOrTestRef: `QUAL-FB-${String(index + 1)}`, failureScenarioRefs: [id(variant, `FSS-SCEN-${String((index % scenarioCount) + 1).padStart(2, "0")}`)], effectivenessBasis: "Barrier survives the modeled exposure duration with penetration and closure failure explicitly quantified." }));
  const scenarios = Array.from({ length: scenarioCount }, (_, index) => ({ ...record(variant, `FSS-SCEN-${String(index + 1).padStart(2, "0")}`, `${ignitionSources[index]!.name} retained fire scenario`, "Retained source-to-target scenario integrating ignition, fire growth, target damage, detection, suppression, barriers, circuit effects, and plant response.", "Scenario construction follows the selected source, complete target set, qualified fire model, suppression treatment, and event-sequence mapping.", [id(variant, "EVID-FIREMODEL"), id(variant, "EVID-WALK")]), physicalAnalysisUnitRefs: index === 4 ? [pauIds[index]!, pauIds[(index + 1) % pauIds.length]!] : [pauIds[index]!], ignitionSourceRefs: [ignitionSources[index]!.uuid], damageTargetSetRef: targetSets[index]!.uuid, fireModelAnalysisRef: fireModels[index]!.uuid, detectionSuppressionAssessmentRef: suppression[index]!.uuid, barrierAssessmentRefs: index < 4 ? [barrierAssessments[index]!.uuid] : [], initiatingEventRefs: [initiatorSelections[index % initiatorSelections.length]!.initiatingEventRef], plantOperatingStateRefs: mef.baselinePra!.plantOperatingStateRefs, reactorUnitRefs: [mef.baselinePra!.reactorUnitRefs[index % context.modules] ?? "UNIT-1"], radioactiveMaterialSourceRefs: [index === 5 ? "SPENT-FUEL-INVENTORY" : "REACTOR-INVENTORY"], scenarioType: index === 3 ? "CONTROL_ROOM_ABANDONMENT" as const : index === 4 ? "MULTI_COMPARTMENT" as const : index === 5 ? "STRUCTURAL_STEEL" as const : "SINGLE_COMPARTMENT" as const, commandAndControlTransferRequired: index === 3, structuralSteelFailureEvaluated: index === 5, smokeDamageEvaluation: "Smoke exposure to sensitive electronics and habitability is evaluated using ventilation and detector timing.", secondaryCombustibleTreatment: "Administrative limit plus 95th-percentile fixed combustible loading is included.", disposition: "QUANTIFIED" as const }));
  const fssScreening = ignitionSources.slice(6).map((source, index) => ({ ...record(variant, `FSS-SCREEN-${String(index + 1).padStart(2, "0")}`, `${source.name} detailed-screening disposition`, "Documents why the lower-risk source does not create an additional quantified scenario family.", "Bounding ignition frequency and conditional-damage treatment remain below the approved screening threshold.", [id(variant, "EVID-FHA")]), screenedObjectType: "IGNITION_SOURCE" as const, screenedObjectRefs: [source.uuid], criterion: "APPROVED_ADDITIONAL" as const, disposition: "SCREENED" as const, conservativeAssumptions: ["No suppression credit", "Upper-bound heat release rate", "All nearby non-PRA targets ignored only after walkdown confirmation"], quantitativeValue: 7.5e-9 + index * 1e-9, quantitativeUnit: "/plant-year", threshold: 1e-8, affectedEventSequenceFamilyRefs: [] }));
  const scenarioInvestigation = { ...record(variant, "FSS-INV-001", "Ignition source, target, and fire-protection walkdown", "Confirms source geometry, intervening combustibles, target separation, barrier condition, detection, suppression, access, and smoke paths.", "All retained scenarios and a risk-informed sample of screened sources were inspected.", [id(variant, "EVID-WALK")]), investigationType: "WALKDOWN" as const, scope: "Retained PAUs, ignition sources, target sets, barriers, detection and suppression", plantOperatingStateRefs: mef.baselinePra!.plantOperatingStateRefs, locations: paus.slice(0, 6).map((item) => item.name), participants: ["Fire modeling lead", "Fire protection engineer", "Electrical engineer", "Systems engineer", "HRA analyst", "Operations representative"], performedDate: "2026-05-22", observations: ["Cabinet-to-cable target distance corrected by 0.4 m.", "Transient combustible exclusion zone expanded near essential switchgear.", "Alternate brigade access route added for smoke sensitivity."], findingRefs: ["FW-041", "FW-046"], confirmedRecordRefs: [...ignitionSources.map((item) => item.uuid), ...targetSets.map((item) => item.uuid), ...scenarios.map((item) => item.uuid)] };
  const fssUncertainty = uncertainty(variant, "FSS", scenarios.map((item) => item.uuid)); const fssPreop = preop(variant, "FSS", suppression.map((item) => item.uuid));
  mef.scenarioSelectionAndAnalysis = { ignitionSources, damageTargetSets: targetSets, fireScenarios: scenarios, fireModelAnalyses: fireModels, detectionSuppressionAssessments: suppression, barrierAssessments, screeningDecisions: fssScreening, investigations: [scenarioInvestigation], modelUncertainties: [fssUncertainty], preOperationalAssumptions: [fssPreop], documentation: documentation(variant, "FSS", "Internal Fire Scenario Selection and Analysis documentation", "Develop source-to-target scenarios and quantify growth, damage, detection, suppression, barriers, abandonment, smoke, structural effects, and multi-compartment propagation.", "PAUs, sources, equipment, cables, raceways, circuits, fire-protection systems, procedures, geometry, material properties, and walkdowns.", "Scenario taxonomy, damage target grouping, fire modeling, suppression timing, barrier reliability, circuit mapping, HRA context, and detailed screening.", `Six representative retained scenarios cover single-compartment, multi-compartment, control-room-abandonment, and structural-steel challenges.`) };
  cover([...ignitionSources, ...targetSets, ...scenarios, ...fireModels, ...suppression, ...barrierAssessments, ...fssScreening, scenarioInvestigation, fssUncertainty, fssPreop], "FSS");

  const eventData = ["US nuclear operating experience", "International nuclear fire events", "Nonnuclear industrial electrical fires", "Reference-design equipment exposure"].map((name, index) => ({ ...record(variant, `FIGN-DATA-${String(index + 1).padStart(2, "0")}`, name, `Qualified fire-event data supporting source-category ignition frequencies for ${context.reactorType}.`, "Events, exposure, inclusion, exclusion, and technology applicability were independently reviewed.", [id(variant, "EVID-IGN")]), sourceType: index === 2 ? "NONNUCLEAR_INDUSTRY" as const : index === 3 ? "PLANT_SPECIFIC" as const : "NUCLEAR_INDUSTRY" as const, reference: `FIRE-DATA-${String(index + 1).padStart(2, "0")}-2026`, eventCount: [119, 37, 214, 0][index]!, exposurePlantYears: [1850, 612, 3200, 4.5][index]!, applicableTechnology: index === 3 ? context.reactorType : "Light-water reactor data screened for source-category and duty applicability", inclusionCriteria: ["Fire with verified ignition source", "Source category and exposure identifiable", "Within plant operating boundary"], exclusionCriteria: ["Incipient event with no sustained combustion", "External wildfire", "Duplicate report"], applicabilityAssessment: index === 3 ? "Design exposure provides population and duty factors; no operating-event update is credited pre-operationally." : "Source physics and electrical/mechanical duty are applicable after technology screening.", availableForReview: true }));
  const experience = ["Reference fleet review", "Construction and commissioning fire review", "Designer operating-experience review"].map((name, index) => ({ ...record(variant, `FIGN-EXP-${String(index + 1).padStart(2, "0")}`, name, "Reviews applicable plant and fleet fire experience for outliers and frequency-update triggers.", "The review period, exposure, event classification, and outlier test are controlled and reproducible.", [id(variant, "EVID-IGN")]), reviewPeriod: index === 0 ? "2000-2025" : index === 1 ? "2023-2026" : "1995-2025", plantYears: index === 1 ? 0 : 84 + index * 22, fireEventRefs: index === 1 ? ["COMM-FIRE-003"] : ["OE-FIRE-014", "OE-FIRE-027"], outlierDetected: false, outlierBasis: "Posterior predictive check does not identify a statistically significant high-frequency source category.", frequencyUpdateRequired: index === 2 }));
  const frequencyEstimates = ignitionSources.map((source, index) => ({ ...record(variant, `FIGN-EST-${String(index + 1).padStart(2, "0")}`, `${source.name} annual ignition frequency`, "Allocates the controlled source-category frequency to the specific ignition source and PAU.", "Apportionment uses source count, electrical duty, power level, occupancy, maintenance exposure, and transient controls while preserving the plant-wide total.", [id(variant, "EVID-IGN")]), ignitionSourceRef: source.uuid, physicalAnalysisUnitRef: source.physicalAnalysisUnitRef, frequencyGroupRef: source.ignitionFrequencyGroupRef, apportionmentFactors: [{ uuid: id(variant, `FIGN-AP-${String(index + 1)}-1`), factorName: "Source population fraction", value: 0.08 + index * 0.01, basis: "Controlled equipment census" }, { uuid: id(variant, `FIGN-AP-${String(index + 1)}-2`), factorName: "Energetic duty factor", value: 0.65 + (index % 3) * 0.1, basis: "At-power operating profile" }], meanFrequencyPerPlantYear: 2.4e-4 + index * 4.2e-5, fifthPercentile: 6.5e-5 + index * 8e-6, ninetyFifthPercentile: 7.8e-4 + index * 1.1e-4, uncertaintyDistribution: "Lognormal combined category and apportionment uncertainty", preservesPlantWideFrequency: true }));
  const reconciliation = [{ ...record(variant, "FIGN-REC-001", "Source-to-group frequency reconciliation", "Confirms that all source estimates sum to their controlled group frequencies after availability and duty adjustments.", "Automated calculation uses double precision and an acceptance tolerance of 0.1 percent.", [id(variant, "EVID-IGN")]) }, { ...record(variant, "FIGN-REC-002", "Plant-wide ignition-frequency reconciliation", "Confirms that all apportioned source frequencies preserve the updated plant-wide frequency and no source is double counted.", "Independent spreadsheet and PRA-model totals agree within numerical precision.", [id(variant, "EVID-IGN")]) }];
  const fignUncertainty = uncertainty(variant, "FIGN", frequencyEstimates.map((item) => item.uuid)); const fignPreop = preop(variant, "FIGN", experience.map((item) => item.uuid));
  mef.ignitionFrequency = { eventDataSources: eventData, frequencyGroups, plantExperienceReviews: experience, frequencyEstimates, reconciliationChecks: reconciliation, modelUncertainties: [fignUncertainty], preOperationalAssumptions: [fignPreop], documentation: documentation(variant, "FIGN", "Internal Fire Ignition Frequency documentation", "Qualify event data, develop source-category frequencies, review plant experience, apportion frequencies to sources and PAUs, and reconcile totals.", "Nuclear and nonnuclear fire events, exposure, source populations, duty factors, availability, plant experience, and uncertainty data.", "Event screening, technology applicability review, Bayesian updating, source apportionment, uncertainty combination, and exact total reconciliation.", `${String(frequencyEstimates.length)} source-level estimates preserve the controlled plant-wide frequency and provide uncertainty percentiles.`) };
  cover([...eventData, ...frequencyGroups, ...experience, ...frequencyEstimates, ...reconciliation, fignUncertainty, fignPreop], "FIGN");

  const circuits = equipment.map((item, index) => ({ ...record(variant, `FCF-CKT-${String(index + 1).padStart(2, "0")}`, `${item.equipmentRef} fire-exposed circuit`, `Circuit whose fire-induced failure can cause ${item.fireFailureModes.join(" or ").toLowerCase()} of ${item.equipmentRef}.`, "Control schematics, cable endpoints, power supplies, protective devices, and routing establish the circuit boundary.", [id(variant, "EVID-SLD"), id(variant, "EVID-CABLE")]), circuitId: `CKT-${String(index + 1).padStart(3, "0")}`, cableRefs: [cableIds[index]!], equipmentRef: item.uuid, circuitFunction: item.creditedFunctions[0]!, powerSupplyRef: `BUS-${String((index % 4) + 1)}`, protectiveDeviceRefs: [`BRKR-${String((index % 4) + 1)}A`], physicalAnalysisUnitRefs: cables[index]!.physicalAnalysisUnitRefs, circuitConfiguration: index % 2 === 0 ? "Energized control conductor with shared return and interposing relay" : "Three-phase power conductor with coordinated upstream breaker" }));
  const evaluations = circuits.map((circuit, index) => ({ ...record(variant, `FCF-EVAL-${String(index + 1).padStart(2, "0")}`, `${circuit.circuitId} failure-mode evaluation`, "Evaluates credible open, ground, inter-conductor, and inter-cable faults and the resulting equipment response.", "Circuit configuration, conductor energization, protective-device behavior, fire scenario, and target damage are explicitly considered.", [id(variant, "EVID-SLD"), id(variant, "EVID-OCP")]), circuitRef: circuit.uuid, fireScenarioRefs: [scenarios[index % scenarios.length]!.uuid], cableRefs: circuit.cableRefs, equipmentFailureMode: index % 3 === 0 ? "SPURIOUS_OPERATION" as const : index % 3 === 1 ? "LOSS_OF_FUNCTION" as const : "SPURIOUS_INDICATION" as const, circuitFailureMode: index % 3 === 0 ? "CONDUCTOR_TO_CONDUCTOR_HOT_SHORT" as const : index % 3 === 1 ? "SHORT_TO_GROUND" as const : "OPEN_CIRCUIT" as const, hotShortDurationCredited: index % 3 === 0, hotShortDurationSeconds: index % 3 === 0 ? 900 + index * 120 : undefined, modelBasicEventRefs: [`BE-CIRCUIT-${String(index + 1)}`], scenarioSpecificCharacteristics: ["Cable damage time from linked fire model", "Circuit energized at damage time", "Protective-device clearing explicitly assessed"] }));
  const probabilities = evaluations.map((evaluation, index) => ({ ...record(variant, `FCF-PROB-${String(index + 1).padStart(2, "0")}`, `${evaluation.name} probability`, "Assigns the scenario-specific circuit failure-mode probability and uncertainty.", "Generic test data are adjusted only for controlled circuit configuration and hot-short duration characteristics.", [id(variant, "EVID-DATA")]), failureModeEvaluationRef: evaluation.uuid, fireScenarioRefs: evaluation.fireScenarioRefs, meanProbability: evaluation.circuitFailureMode === "CONDUCTOR_TO_CONDUCTOR_HOT_SHORT" ? 0.12 + index * 0.01 : 0.82 - index * 0.025, fifthPercentile: 0.035, ninetyFifthPercentile: 0.94, durationProbability: evaluation.hotShortDurationCredited ? 0.31 : undefined, uncertaintyDistribution: "Beta distribution with lognormal scenario adjustment", genericDataRef: "NUREG/CR fire circuit failure data set", scenarioSpecificAdjustment: "Adjustment reflects conductor count, energization, grounding, protection, cable construction, and duration.", bounding: index === 7 }));
  const fcfUncertainty = uncertainty(variant, "FCF", evaluations.map((item) => item.uuid)); const fcfPreop = preop(variant, "FCF", circuits.slice(6).map((item) => item.uuid));
  mef.circuitFailureAnalysis = { circuits, failureModeEvaluations: evaluations, failureProbabilities: probabilities, modelUncertainties: [fcfUncertainty], preOperationalAssumptions: [fcfPreop], documentation: documentation(variant, "FCF", "Internal Fire Circuit Failure Analysis documentation", "Define fire-exposed circuits and quantify loss of function, spurious operations, spurious indications, hot shorts, ground faults, opens, and duration effects.", "Schematics, cable routing, conductor data, protective-device coordination, equipment response, fire scenarios, and circuit-failure data.", "Circuit boundary tracing, failure-mode and effects review, hot-short duration analysis, protection evaluation, probability assignment, and uncertainty characterization.", `${String(circuits.length)} circuits and ${String(evaluations.length)} scenario-specific failure modes are quantified and linked to plant-response logic.`) };
  cover([...circuits, ...evaluations, ...probabilities, fcfUncertainty, fcfPreop], "FCF");

  const actions = ["Diagnose fire and trip affected train", "Align alternate shutdown cooling", "Transfer control to remote shutdown panel", "Isolate smoke-affected ventilation", "Establish alternate DC supply", "Restore spent-fuel cooling"].map((name, index) => ({ ...record(variant, `FHR-ACT-${String(index + 1).padStart(2, "0")}`, name, `Fire-specific crew action to ${name.toLowerCase()} following the linked scenario cues.`, "Procedure review, talk-through, simulator exercise, route walkdown, cue review, and timing analysis establish the modeled action.", [id(variant, "EVID-HRA"), id(variant, "EVID-OPS")]), actionType: index === 2 ? "CONTROL_ROOM_ABANDONMENT" as const : index === 5 ? "RECOVERY" as const : index === 0 ? "BASELINE_MODIFIED" as const : "FIRE_SAFE_SHUTDOWN" as const, procedureRefs: [index === 2 ? "AOP-FIRE-ABANDON" : `FSP-${String(index + 1).padStart(3, "0")}`], fireScenarioRefs: [scenarios[index]!.uuid], eventSequenceRefs: [eventSequences[index % eventSequences.length]!.uuid], requiredLocation: index === 2 ? "Remote shutdown panel" : index === 5 ? "Fuel Handling Building local panel" : "Main control room and local equipment station", cuesAndIndications: ["Validated fire alarm location", "Diverse equipment status indication", "Procedure entry condition"], availableTimeMinutes: [12, 28, 18, 22, 35, 48][index]!, executionTimeMinutes: [4.5, 11, 9, 7, 14, 19][index]!, accessRoute: index === 2 ? "Protected east stair to remote shutdown panel; alternate exterior route available" : "Primary route remains outside the fire PAU with smoke-controlled alternate access", requiredEquipment: ["Portable radio", "Flashlight", index === 2 ? "Self-contained breathing apparatus" : "Fire response key set"], crew: index === 2 ? "Shift manager, reactor operator, and auxiliary operator" : "Control-room operator and assigned auxiliary operator" }));
  const hfes = actions.map((action, index) => ({ ...record(variant, `FHR-HFE-${String(index + 1).padStart(2, "0")}`, `Failure to ${action.name.toLowerCase()}`, `Represents failure, delay, or incorrect execution of ${action.name.toLowerCase()} within the available time.`, "The HFE boundary is compatible with event-sequence logic and avoids double counting equipment and cue failures.", [id(variant, "EVID-HRA")]), humanActionRef: action.uuid, basicEventRef: `HFE-FIRE-${String(index + 1).padStart(2, "0")}`, failureDefinition: `Crew does not complete ${action.name.toLowerCase()} before the linked success-criterion deadline.`, fireScenarioRefs: action.fireScenarioRefs, affectedEventSequenceRefs: action.eventSequenceRefs, physicalAnalysisUnitRefs: [pauIds[index]!], plantOperatingStateRefs: mef.baselinePra!.plantOperatingStateRefs, reactorUnitRefs: [mef.baselinePra!.reactorUnitRefs[index % context.modules] ?? "UNIT-1"], radioactiveMaterialSourceRefs: [index === 5 ? "SPENT-FUEL-INVENTORY" : "REACTOR-INVENTORY"] }));
  const contexts = hfes.map((hfe, index) => ({ ...record(variant, `FHR-CTX-${String(index + 1).padStart(2, "0")}`, `${hfe.name} performance context`, "Captures the fire-specific performance-shaping conditions affecting diagnosis and execution.", "Context was developed jointly by HRA, operations, fire protection, electrical, and systems analysts.", [id(variant, "EVID-HRA"), id(variant, "EVID-WALK")]), humanFailureEventRef: hfe.uuid, fireScenarioRef: hfe.fireScenarioRefs[0]!, smokeAndHeatConditions: index === 2 ? "Heavy smoke in control-room access corridor; SCBA required after abandonment trigger." : "Localized smoke outside the primary action route; thermal exposure below tenability criterion.", lightingAndVisibility: "Emergency lighting available; visibility sensitivity evaluated for smoke migration.", alarmAndCueReliability: "Addressable alarm and one diverse plant parameter are required before action.", spuriousIndicationEffects: index % 2 === 0 ? "One misleading indication is assumed; procedure requires diverse confirmation." : "No credited indication shares the damaged cable set.", accessAndHabitability: "Primary and alternate routes were walked; travel time includes protective-equipment donning.", workloadAndStress: "Concurrent fire response, reactor response, communications, and command transfer represented.", communications: "Dedicated radio channel plus hardwired remote-panel telephone.", procedureQuality: "Validated symptom-based procedure with explicit fire-area branching.", staffingAndTraining: "Minimum shift staffing plus trained fire brigade; annual simulator scenario required." }));
  const heps = hfes.map((hfe, index) => ({ ...record(variant, `FHR-HEP-${String(index + 1).padStart(2, "0")}`, `${hfe.name} HEP estimate`, "Quantifies diagnosis and execution failure for the linked fire context.", "Method applies fire-specific timing, cue, procedure, access, workload, stress, and dependency factors.", [id(variant, "EVID-HRA"), id(variant, "EVID-DATA")]), humanFailureEventRef: hfe.uuid, performanceContextRef: contexts[index]!.uuid, method: "Fire HRA adaptation of EPRI HRA Calculator with time-reliability correlation and structured expert review", screeningValueUsed: false, meanHep: [0.045, 0.082, 0.16, 0.071, 0.095, 0.13][index]!, fifthPercentile: [0.012, 0.021, 0.055, 0.018, 0.028, 0.041][index]!, ninetyFifthPercentile: [0.15, 0.25, 0.39, 0.22, 0.29, 0.36][index]!, uncertaintyDistribution: "Lognormal bounded at one", dependencyGroupRefs: [id(variant, `FHR-DEP-${String((index % 3) + 1).padStart(2, "0")}`)], recoveryCredited: index === 0 || index === 3, feasibilityDemonstrated: true }));
  const dependencies = Array.from({ length: 3 }, (_, index) => ({ ...record(variant, `FHR-DEP-${String(index + 1).padStart(2, "0")}`, `Fire HFE dependency group ${String(index + 1)}`, "Combines sequential fire actions sharing crew, cues, location, or timing.", "Dependency level follows explicit commonality and recovery assessment rather than independent multiplication.", [id(variant, "EVID-HRA")]), humanFailureEventRefs: [hfes[index * 2]!.uuid, hfes[index * 2 + 1]!.uuid], sharedCrew: true, sharedCues: index !== 2, sharedLocation: index === 0, temporalRelationship: "Second action begins after initial diagnosis and shares the evolving fire response context.", dependencyLevel: index === 0 ? "MODERATE" as const : index === 1 ? "HIGH" as const : "LOW" as const, jointHep: [0.016, 0.041, 0.027][index]! }));
  const confirmations = actions.map((action, index) => ({ ...record(variant, `FHR-CONF-${String(index + 1).padStart(2, "0")}`, `${action.name} feasibility confirmation`, "Documents field or simulator confirmation of procedure, cue, timing, route, communication, equipment, staffing, and habitability assumptions.", "Confirmation uses representative trained crews and conservative plant/fire conditions.", [id(variant, "EVID-HRA")]), humanActionRefs: [action.uuid], confirmationType: index % 3 === 0 ? "SIMULATOR" as const : index % 3 === 1 ? "WALKDOWN" as const : "TALK_THROUGH" as const, participants: ["Licensed operator", "Auxiliary operator", "HRA analyst", "Fire protection engineer"], performedDate: `2026-04-${String(10 + index).padStart(2, "0")}`, findings: index === 2 ? ["Add SCBA donning cue to procedure.", "Install route marker at east stair landing."] : ["Action completed within modeled execution time.", "Required cue and equipment available."], feasible: true }));
  const fhrUncertainty = uncertainty(variant, "FHR", heps.map((item) => item.uuid)); const fhrPreop = preop(variant, "FHR", confirmations.map((item) => item.uuid));
  mef.humanReliabilityAnalysis = { humanActions: actions, humanFailureEvents: hfes, performanceContexts: contexts, hepEstimates: heps, dependencyAssessments: dependencies, confirmations, modelUncertainties: [fhrUncertainty], preOperationalAssumptions: [fhrPreop], documentation: documentation(variant, "FHR", "Internal Fire Human Reliability Analysis documentation", "Identify fire-specific actions and HFEs, characterize context, confirm feasibility, quantify HEPs and recovery, and model dependencies.", "Fire scenarios, event sequences, procedures, alarms, indications, cable effects, timing, routes, habitability, staffing, training, simulator, and walkdowns.", "Joint analyst-operator review, timing decomposition, performance-shaping-factor assessment, fire HRA quantification, dependency analysis, and field confirmation.", `Six fire HFEs are quantified with scenario-specific context, uncertainty, recovery, dependency, and feasibility evidence.`) };
  cover([...actions, ...hfes, ...contexts, ...heps, ...dependencies, ...confirmations, fhrUncertainty, fhrPreop], "FHR");

  const runs = ["F-R0.9 sensitivity model", "F-R1.0 controlled model"].map((name, index) => ({ ...record(variant, `FESQ-RUN-${String(index + 1).padStart(2, "0")}`, name, "Controlled end-to-end Internal Fire quantification run with convergence, dependency, and uncertainty settings recorded.", "Run manifest, checksums, software qualification, truncation study, and independent review establish reproducibility.", [id(variant, "EVID-PRA"), id(variant, "EVID-DATA")]), modelVersion: index === 0 ? "F-R0.9" : "F-R1.0", calculationDate: index === 0 ? "2026-06-17" : "2026-06-30", software: "SAPHIRE-compatible OpenPRA quantifier", softwareVersion: "8.4.0 / engine 1.7.2", truncationLevel: index === 0 ? 1e-11 : 1e-12, lowerTruncationCheck: index === 0 ? 1e-12 : 1e-13, convergenceMetric: index === 0 ? 0.009 : 0.0028, convergenceCriterion: 0.01, converged: true, includedFireScenarioRefs: scenarios.map((item) => item.uuid), excludedFireScenarioRefs: fssScreening.flatMap((item) => item.screenedObjectRefs), dependencyTreatment: "Scenario exclusivity, shared ignition-frequency parameters, barrier/suppression common cause, circuit correlations, and HFE dependencies are explicitly modeled.", methodLimitations: ["Rare higher-order circuit combinations below truncation are bounded by the lower-truncation check."] }));
  const scenarioResults = scenarios.map((scenario, index) => { const mean = frequencyEstimates[index]!.meanFrequencyPerPlantYear * fireModels[index]!.conditionalTargetDamageProbability * (0.055 + index * 0.012); return { ...record(variant, `FESQ-SCEN-${String(index + 1).padStart(2, "0")}`, `${scenario.name} quantified result`, "Combines ignition frequency, conditional damage, suppression, circuit, human, and sequence probabilities for the retained scenario.", "Every multiplicative factor and model branch traces to a controlled technical record.", [id(variant, "EVID-PRA")]), quantificationRunRef: runs[1]!.uuid, fireScenarioRef: scenario.uuid, initiatingEventRef: scenario.initiatingEventRefs[0]!, ignitionFrequencyRef: frequencyEstimates[index]!.uuid, conditionalDamageProbability: fireModels[index]!.conditionalTargetDamageProbability, circuitFailureProbabilityRefs: [probabilities[index]!.uuid], humanFailureEventRefs: [hfes[index]!.uuid], conditionalSequenceProbability: 0.055 + index * 0.012, meanFrequencyPerPlantYear: mean, fifthPercentile: mean * 0.28, ninetyFifthPercentile: mean * 3.4, eventSequenceFamilyRefs: [`ESF-FIRE-${String((index % 4) + 1)}`], releaseCategoryRefs: [`RC-${String((index % 3) + 1)}`], screened: false }; });
  const familyResults = Array.from({ length: 4 }, (_, index) => { const members = scenarioResults.filter((_, i) => i % 4 === index); const mean = members.reduce((sum, item) => sum + item.meanFrequencyPerPlantYear, 0); return { ...record(variant, `FESQ-FAM-${String(index + 1).padStart(2, "0")}`, `Fire event-sequence family ${String(index + 1)} result`, "Aggregates compatible fire scenarios by initiating event, plant response, end state, and release category.", "Scenario aggregation preserves exclusivity, common parameters, operating-state coverage, and unit/source attribution.", [id(variant, "EVID-PRA")]), quantificationRunRef: runs[1]!.uuid, eventSequenceFamilyRef: `ESF-FIRE-${String(index + 1)}`, fireScenarioRefs: members.map((item) => item.fireScenarioRef), initiatingEventRefs: [...new Set(members.map((item) => item.initiatingEventRef))], plantOperatingStateRefs: mef.baselinePra!.plantOperatingStateRefs, reactorUnitRefs: mef.baselinePra!.reactorUnitRefs, radioactiveMaterialSourceRefs: index === 3 ? ["SPENT-FUEL-INVENTORY"] : ["REACTOR-INVENTORY"], releaseCategoryRef: `RC-${String((index % 3) + 1)}`, meanFrequencyPerPlantYear: mean, medianFrequencyPerPlantYear: mean * 0.82, fifthPercentileFrequencyPerPlantYear: mean * 0.24, ninetyFifthPercentileFrequencyPerPlantYear: mean * 3.8, dominantCutsetRefs: [`CUTSET-FIRE-${String(index + 1)}-01`, `CUTSET-FIRE-${String(index + 1)}-02`] }; });
  const contributors = Array.from({ length: 12 }, (_, index) => { const result = scenarioResults[index % scenarioResults.length]!; const contribution = result.meanFrequencyPerPlantYear * (0.72 - (index % 4) * 0.12); return { ...record(variant, `FESQ-CONTR-${String(index + 1).padStart(2, "0")}`, `Rank ${String(index + 1)} Internal Fire contributor`, "Ranks a PAU, source, scenario, cable, circuit failure, HFE, equipment item, or sequence family by annual risk contribution.", "Contribution and importance measures are calculated from the controlled quantification baseline.", [id(variant, "EVID-PRA")]), contributorType: ["FIRE_SCENARIO", "PAU", "IGNITION_SOURCE", "CABLE", "CIRCUIT_FAILURE", "HFE", "EQUIPMENT", "EVENT_SEQUENCE_FAMILY"][index % 8] as InternalFirePRA["eventSequenceQuantification"]["riskContributors"][number]["contributorType"], contributorRef: [result.fireScenarioRef, scenarios[index % scenarios.length]!.physicalAnalysisUnitRefs[0]!, scenarios[index % scenarios.length]!.ignitionSourceRefs[0]!, cableIds[index % cableIds.length]!, probabilities[index % probabilities.length]!.uuid, hfes[index % hfes.length]!.uuid, equipment[index % equipment.length]!.uuid, `ESF-FIRE-${String((index % 4) + 1)}`][index % 8]!, meanFrequencyContributionPerPlantYear: contribution, fractionalContribution: Math.min(0.42, 0.24 - index * 0.012 + (index % 3) * 0.04), fussellVesely: Math.min(0.5, 0.27 - index * 0.01), riskAchievementWorth: 1.8 + (12 - index) * 0.42, rank: index + 1 }; });
  const uncertaintyResults = familyResults.map((family, index) => ({ ...record(variant, `FESQ-UNCRES-${String(index + 1).padStart(2, "0")}`, `${family.name} uncertainty result`, "Propagates ignition, fire growth, suppression, barrier, circuit, random-failure, HRA, and model uncertainty for the family.", "Latin-hypercube sampling preserves parameter dependencies and evaluates documented model alternatives.", [id(variant, "EVID-DATA")]), quantificationRunRef: runs[1]!.uuid, resultRef: family.uuid, uncertaintySourceRefs: [fignUncertainty.uuid, fssUncertainty.uuid, fcfUncertainty.uuid, fhrUncertainty.uuid], propagationMethod: "20,000-sample Latin hypercube with correlated parameter groups and discrete model-alternative cases", sampleCount: 20000, mean: family.meanFrequencyPerPlantYear, median: family.medianFrequencyPerPlantYear, fifthPercentile: family.fifthPercentileFrequencyPerPlantYear, ninetyFifthPercentile: family.ninetyFifthPercentileFrequencyPerPlantYear, modelUncertaintySensitivityRefs: [id(variant, `FESQ-SENS-${String(index + 1)}`)] }));
  const quantReviews = ["CORRECTNESS", "COMPLETENESS", "CONSISTENCY", "CONVERGENCE", "TRACEABILITY"].map((reviewType, index) => ({ ...record(variant, `FESQ-REV-${String(index + 1).padStart(2, "0")}`, `${reviewType.toLowerCase()} quantification review`, `Independent ${reviewType.toLowerCase()} review of the controlled Internal Fire quantification.`, "Review checklist, calculation comparison, model query, and finding closure are retained in the package.", [id(variant, "EVID-PEER")]), quantificationRunRef: runs[1]!.uuid, reviewType: reviewType as InternalFirePRA["eventSequenceQuantification"]["reviews"][number]["reviewType"], checksPerformed: ["Manifest and checksum verified", "Scenario and family totals reconciled", "Convergence and lower-truncation check confirmed", "Random sample traced to source records"], result: "PASS" as const, openItems: [] }));
  const fesqScreen = fssScreening.map((item, index) => ({ ...item, uuid: id(variant, `FESQ-SCREEN-${String(index + 1)}`), code: `FESQ-SCREEN-${String(index + 1).padStart(2, "0")}`, name: `${item.name} quantification confirmation`, screenedObjectType: "FIRE_SCENARIO" as const, screenedObjectRefs: [`SCREENED-SCEN-${String(index + 1)}`] }));
  const fesqUncertainty = uncertainty(variant, "FESQ", familyResults.map((item) => item.uuid)); const fesqPreop = preop(variant, "FESQ", [runs[1]!.uuid]);
  const subelementUncertainties = [ppUncertainty, fesUncertainty, fcsUncertainty, fqlsUncertainty, fprmUncertainty, fssUncertainty, fignUncertainty, fcfUncertainty, fhrUncertainty, fesqUncertainty];
  const quantificationIntegratedUncertainties = subelementUncertainties.map((item, index) => ({ ...item, uuid: id(variant, `FESQ-INTEGRATED-UNC-${String(index + 1).padStart(2, "0")}`), code: `FESQ-INTEGRATED-UNC-${String(index + 1).padStart(2, "0")}`, name: `${item.sourceSubelement} uncertainty integrated in quantification`, implementsSrs: [] }));
  mef.eventSequenceQuantification = { quantificationRuns: runs, scenarioResults, eventSequenceFamilyResults: familyResults, riskContributors: contributors, uncertaintyResults, reviews: quantReviews, screeningDecisions: fesqScreen, integratedModelUncertainties: quantificationIntegratedUncertainties, preOperationalAssumptions: [fesqPreop], documentation: documentation(variant, "FESQ", "Internal Fire Event Sequence Quantification documentation", "Quantify retained scenarios and event-sequence families with dependencies, uncertainty, screening, convergence, importance, and full traceability.", "All controlled Internal Fire subelement outputs, baseline PRA logic and data, software, run settings, screening thresholds, and uncertainty models.", "Scenario-conditioned quantification, sequence-family aggregation, dependency modeling, truncation study, uncertainty propagation, importance analysis, and independent review.", `Six scenario results aggregate into four event-sequence families; the controlled run converges at 1E-12 truncation with 20,000-sample uncertainty propagation.`) };
  cover([...runs, ...scenarioResults, ...familyResults, ...contributors, ...uncertaintyResults, ...quantReviews, ...fesqScreen, fesqUncertainty, fesqPreop], "FESQ");

  type TransferSeed = Omit<InternalFirePraInterfaceTransferItem, "uuid" | "status">;
  const makeInterface = (config: Pick<InternalFirePraInterfaceRecord, "technicalElementCode" | "technicalElementName" | "direction" | "role" | "payloadType" | "columns"> & { code: string; description: string; basis: string; transfers: TransferSeed[] }): InternalFirePraInterfaceRecord => {
    const transferItems = config.transfers.map((item, index) => ({ ...item, uuid: id(variant, `${config.code}-ITEM-${String(index + 1).padStart(2, "0")}`), status: "CONTROLLED" as const }));
    return {
      ...record(variant, config.code, `${config.technicalElementCode} ${config.direction.toLowerCase()} interface`, config.description, config.basis, [id(variant, "EVID-CONFIG")]),
      technicalElementCode: config.technicalElementCode, technicalElementName: config.technicalElementName, direction: config.direction, role: config.role,
      producer: config.direction === "INPUT" ? config.technicalElementCode : "F", consumer: config.direction === "INPUT" ? "F" : config.technicalElementCode,
      payloadType: config.payloadType, columns: config.columns, transferItems, standardRequirementRefs: [],
      producerRefs: transferItems.map((item) => item.recordRef), consumerRefs: Array.from(new Set(transferItems.flatMap((item) => item.destinationRefs))), transferBasis: "",
      consistencyChecks: ["Source revision and checksum controlled", "Record identifiers resolve", "At-power scope, units, and annualization reconciled", "Destination import count and values verified"], consistent: true, openItems: [],
    };
  };
  const posProfiles = [
    ["POS-FULL-POWER", "Full-power operation", "POWER OPERATION", "7,884 h · 90.0%", `${String(context.modules)} reactor unit(s) at rated power`, "Reactor · spent fuel · radwaste", "Normal alignment; all credited fire barriers, detection, suppression, ventilation, and safe-shutdown trains available"],
    ["POS-POWER-ASCENSION", "Power ascension", "POWER ASCENSION", "438 h · 5.0%", `${String(context.modules)} reactor unit(s) between minimum power and rated power`, "Reactor · spent fuel · radwaste", "Startup testing and equipment alignments represented; transient-combustible and impairment controls active"],
    ["POS-POWER-DESCENSION", "Power descension", "POWER DESCENSION", "438 h · 5.0%", `${String(context.modules)} reactor unit(s) reducing power before shutdown entry`, "Reactor · spent fuel · radwaste", "Normal shutdown alignment with credited alternate cooling and electrical divisions available"],
  ];
  const posTransfers: TransferSeed[] = posProfiles.map(([recordRef, name, mode, exposure, unitScope, materialSources, fireConfiguration], index) => ({
    recordRef: String(recordRef), name: String(name), sourceModelRef: `${mef.baselinePra!.modelReference} · POS basis R4.2`,
    destinationRefs: [paus[index]!.uuid, scenarios[index]!.uuid, frequencyEstimates[index]!.uuid],
    values: [String(mode), String(exposure), String(unitScope), String(materialSources), String(fireConfiguration)], evidenceRefs: [id(variant, "EVID-PRA"), id(variant, "EVID-OPS"), id(variant, "EVID-CONFIG")],
  }));
  const baselineInitiatorFrequencies = [1.8e-2, 1.1e-2, 4.2e-3, 6.8e-3];
  const ieTransfers: TransferSeed[] = initiatorSelections.map((item, index) => ({
    recordRef: item.sourceTechnicalElementRef, name: item.name.replace(/ selection$/i, ""), sourceModelRef: `${mef.baselinePra!.modelReference} · Initiating Events R6.1`,
    destinationRefs: [item.uuid, initiatingModels[index % initiatingModels.length]!.uuid, ...scenarios.filter((scenario) => scenario.initiatingEventRefs.includes(item.initiatingEventRef)).map((scenario) => scenario.uuid)],
    values: [titleCaseForSeed(item.selectionType), `${item.sourceTechnicalElementRef} → ${item.initiatingEventRef}`, `${scientific(baselineInitiatorFrequencies[index]!)} /plant-year`, item.fireCausingEquipmentRefs.map((ref) => equipment.find((candidate) => candidate.uuid === ref)?.equipmentRef ?? ref).join(" · "), `ET-FIRE-${String(index + 1).padStart(2, "0")}`, `${item.affectedReactorUnitRefs.join(" · ")} · ${item.affectedRadioactiveMaterialSourceRefs.join(" · ")}`],
    evidenceRefs: [id(variant, "EVID-PRA"), id(variant, "EVID-SLD")],
  }));
  const esTransfers: TransferSeed[] = eventSequences.map((item) => ({
    recordRef: item.baselineEventSequenceRef, name: item.name, sourceModelRef: `${mef.baselinePra!.modelReference} · Event Sequences R7.0`, destinationRefs: [item.uuid, ...item.fireScenarioRefs, ...item.eventSequenceFamilyRefs],
    values: [item.initiatingEventRef, item.baselineEventSequenceRef, item.topEvents.join(" · "), item.endStates.join(" · "), titleCaseForSeed(item.modelTreatment), item.eventSequenceFamilyRefs.join(" · ")], evidenceRefs: [id(variant, "EVID-PRA"), id(variant, "EVID-OPS")],
  }));
  const scTransfers: TransferSeed[] = successCriteria.map((item) => ({
    recordRef: item.baselineSuccessCriterionRef, name: item.name, sourceModelRef: `${mef.baselinePra!.modelReference} · Success Criteria R5.4`, destinationRefs: [item.uuid, item.analysisRef, ...item.requiredSystemTrainRefs],
    values: [item.function, item.successDefinition, `${String(item.missionTimeHours)} h`, item.requiredSystemTrainRefs.join(" · "), item.analysisRef, item.fireSpecificConditions.join(" · ")], evidenceRefs: [id(variant, "EVID-PRA"), id(variant, "EVID-FHA")],
  }));
  const syTransfers: TransferSeed[] = systemMods.map((item) => ({
    recordRef: item.baselineModelRef, name: item.name, sourceModelRef: `${mef.baselinePra!.modelReference} · Systems R8.1`, destinationRefs: [item.uuid, ...item.fireEquipmentRefs, ...item.cableRefs, ...item.spuriousOperationRefs],
    values: [item.systemRef, item.baselineModelRef, item.affectedBasicEventRefs.join(" · "), `${item.fireEquipmentRefs.length} equipment · ${item.cableRefs.length} cable(s)`, item.humanFailureEventRefs.join(" · "), item.modelChange], evidenceRefs: [id(variant, "EVID-PRA"), id(variant, "EVID-CABLE"), id(variant, "EVID-SLD")],
  }));
  const hrTransfers: TransferSeed[] = actions.map((item, index) => ({
    recordRef: `HR-BASE-${String(index + 1).padStart(2, "0")}`, name: item.name, sourceModelRef: `${mef.baselinePra!.modelReference} · Human Reliability R5.8`, destinationRefs: [item.uuid, hfes[index]!.uuid, heps[index]!.uuid, contexts[index]!.uuid],
    values: [titleCaseForSeed(item.actionType), item.procedureRefs.join(" · "), hfes[index]!.basicEventRef, `${scientific(heps[index]!.meanHep)} mean · ${scientific(heps[index]!.fifthPercentile)} to ${scientific(heps[index]!.ninetyFifthPercentile)}`, `${item.crew} · ${item.requiredLocation}`, item.fireScenarioRefs.join(" · ")], evidenceRefs: [id(variant, "EVID-HRA"), id(variant, "EVID-OPS")],
  }));
  const ignitionDataTransfers: TransferSeed[] = frequencyGroups.map((item) => ({
    recordRef: item.uuid, name: `${item.sourceCategory} ignition-frequency parameter`, sourceModelRef: "Data Analysis fire-event parameter library DA-R8.1", destinationRefs: frequencyEstimates.filter((estimate) => estimate.frequencyGroupRef === item.uuid).map((estimate) => estimate.uuid),
    values: ["Ignition frequency", item.sourceCategory, `${scientific(item.updatedFrequencyPerPlantYear)} /plant-year`, item.uncertaintyDistribution, `${(item.plantAvailabilityFactor * 100).toFixed(1)}% at-power availability · ${item.updateMethod.toLowerCase()} update`], evidenceRefs: [id(variant, "EVID-IGN"), id(variant, "EVID-DATA")],
  }));
  const circuitDataTransfers: TransferSeed[] = probabilities.map((item) => {
    const evaluation = evaluations.find((candidate) => candidate.uuid === item.failureModeEvaluationRef)!;
    return { recordRef: item.uuid, name: `${evaluation.circuitFailureMode.toLowerCase().replace(/_/g, " ")} probability`, sourceModelRef: "Data Analysis fire-circuit parameter library DA-R5.1", destinationRefs: [evaluation.uuid, ...item.fireScenarioRefs],
      values: ["Circuit failure mode", titleCaseForSeed(evaluation.circuitFailureMode), `${scientific(item.meanProbability)} /demand${item.durationProbability === undefined ? "" : ` · ${scientific(item.durationProbability)} duration factor`}`, `${scientific(item.fifthPercentile)} to ${scientific(item.ninetyFifthPercentile)} · ${item.uncertaintyDistribution}`, item.scenarioSpecificAdjustment], evidenceRefs: [id(variant, "EVID-DATA"), id(variant, "EVID-SLD")] };
  });
  const esqScenarioTransfers: TransferSeed[] = scenarioResults.map((item) => ({
    recordRef: item.uuid, name: item.name, sourceModelRef: `${runs[1]!.modelVersion} · ${runs[1]!.software} ${runs[1]!.softwareVersion}`, destinationRefs: [`ESQ-IMPORT-${item.fireScenarioRef}`, ...item.eventSequenceFamilyRefs],
    values: [`${scientific(item.meanFrequencyPerPlantYear)} /plant-year`, `${scientific(item.fifthPercentile)} to ${scientific(item.ninetyFifthPercentile)} /plant-year`, scientific(item.conditionalSequenceProbability), item.releaseCategoryRefs.join(" · "), mef.baselinePra!.plantOperatingStateRefs.join(" · "), `Scenario result · ${item.eventSequenceFamilyRefs.join(" · ")}`], evidenceRefs: [id(variant, "EVID-PRA"), id(variant, "EVID-DATA")],
  }));
  const esqFamilyTransfers: TransferSeed[] = familyResults.map((item) => ({
    recordRef: item.uuid, name: item.name, sourceModelRef: `${runs[1]!.modelVersion} · ${runs[1]!.software} ${runs[1]!.softwareVersion}`, destinationRefs: [`ESQ-IMPORT-${item.eventSequenceFamilyRef}`],
    values: [`${scientific(item.meanFrequencyPerPlantYear)} /plant-year`, `${scientific(item.fifthPercentileFrequencyPerPlantYear)} to ${scientific(item.ninetyFifthPercentileFrequencyPerPlantYear)} /plant-year`, "Aggregated from mutually exclusive scenario branches", item.releaseCategoryRef, item.plantOperatingStateRefs.join(" · "), `${String(item.dominantCutsetRefs.length)} dominant cutsets · ${item.eventSequenceFamilyRef}`], evidenceRefs: [id(variant, "EVID-PRA"), id(variant, "EVID-DATA")],
  }));
  const riTransfers: TransferSeed[] = contributors.map((item) => ({
    recordRef: item.uuid, name: item.name, sourceModelRef: `${runs[1]!.modelVersion} importance calculation`, destinationRefs: [`RI-FIRE-${String(item.rank).padStart(2, "0")}`, ...familyResults.filter((family) => family.fireScenarioRefs.some((ref) => scenarioResults.some((result) => result.fireScenarioRef === ref && [item.contributorRef, result.fireScenarioRef].includes(item.contributorRef)))).map((family) => family.eventSequenceFamilyRef)],
    values: [String(item.rank), titleCaseForSeed(item.contributorType), `${scientific(item.meanFrequencyContributionPerPlantYear)} /plant-year`, `${(item.fractionalContribution * 100).toFixed(1)}%`, `${item.fussellVesely.toFixed(3)} · ${item.riskAchievementWorth.toFixed(2)}`, familyResults.filter((family) => family.fireScenarioRefs.some((ref) => ref === item.contributorRef || scenarios.some((scenario) => scenario.uuid === ref && [scenario.uuid, ...scenario.physicalAnalysisUnitRefs, ...scenario.ignitionSourceRefs].includes(item.contributorRef)))).map((family) => family.eventSequenceFamilyRef).join(" · ") || "Integrated Internal Fire result"], evidenceRefs: [id(variant, "EVID-PRA"), id(variant, "EVID-CONFIG")],
  }));
  mef.integration.interfaces = [
    makeInterface({ code: "F-IF-POS-IN", technicalElementCode: "POS", technicalElementName: "Plant Operating State Analysis", direction: "INPUT", role: "At-power exposure, configuration, unit, and radioactive-material scope", payloadType: "OPERATING_STATE", columns: ["Mode", "Annual exposure", "Unit configuration", "Material sources", "Fire-specific configuration"], description: "Controlled at-power operating states define annual exposure and the plant configurations in which Internal Fire scenarios are evaluated.", basis: "POS records are frozen with the baseline PRA and reconciled to retained PAUs, ignition sources, scenarios, HFEs, and quantified results.", transfers: posTransfers }),
    makeInterface({ code: "F-IF-IE-IN", technicalElementCode: "IE", technicalElementName: "Initiating Event Analysis", direction: "INPUT", role: "Baseline initiating-event groups and frequencies", payloadType: "INITIATING_EVENT", columns: ["Selection", "Baseline / fire initiator", "Baseline mean frequency", "Fire-causing equipment", "Event-tree model", "Units / sources"], description: "Baseline initiating events provide the comparison groups, frequency basis, and event-tree entry points for fire-induced initiators.", basis: "Each fire initiator is mapped to a controlled baseline group or identified as fire-unique with explicit affected equipment, units, and radioactive-material sources.", transfers: ieTransfers }),
    makeInterface({ code: "F-IF-ES-IN", technicalElementCode: "ES", technicalElementName: "Event Sequence Analysis", direction: "INPUT", role: "Baseline event sequences, top events, end states, and sequence families", payloadType: "EVENT_SEQUENCE", columns: ["Initiating event", "Baseline sequence", "Top events", "End states", "Fire treatment", "Sequence family"], description: "Baseline event-sequence models establish the response structure, end states, and sequence families reviewed or modified for fire effects.", basis: "Event-sequence logic is retained where valid and modified when fire damage, circuit behavior, procedures, or control-room abandonment changes plant response.", transfers: esTransfers }),
    makeInterface({ code: "F-IF-SC-IN", technicalElementCode: "SC", technicalElementName: "Success Criteria Analysis", direction: "INPUT", role: "Functional success definitions, mission times, and required trains", payloadType: "SUCCESS_CRITERION", columns: ["Function", "Success definition", "Mission time", "Required trains", "Analysis basis", "Fire-specific conditions"], description: "Success Criteria supplies the functional definitions, engineering analyses, credited trains, and mission times used in the Internal Fire plant-response model.", basis: "Baseline criteria are retained or modified using fire-specific spatial damage, alternate equipment, circuit effects, and action timing.", transfers: scTransfers }),
    makeInterface({ code: "F-IF-SY-IN", technicalElementCode: "SY", technicalElementName: "Systems Analysis", direction: "INPUT", role: "Fault-tree logic, support dependencies, basic events, and equipment functions", payloadType: "SYSTEM_MODEL", columns: ["System", "Baseline model", "Affected basic events", "Fire equipment / cables", "Human actions", "Fire model change"], description: "Systems Analysis supplies controlled fault-tree logic, equipment functions, support dependencies, and random-failure models.", basis: "Internal Fire adds scenario-conditioned spatial damage, cable and circuit failures, split fractions, and fire HFEs without duplicating baseline random failures.", transfers: syTransfers }),
    makeInterface({ code: "F-IF-HR-IN", technicalElementCode: "HR", technicalElementName: "Human Reliability Analysis", direction: "INPUT", role: "Baseline actions, HFEs, procedures, crews, and dependency rules", payloadType: "HUMAN_FAILURE_EVENT", columns: ["Action type", "Procedures", "HFE basic event", "Mean HEP and interval", "Crew / location", "Fire scenarios"], description: "Human Reliability supplies baseline operator actions, HFE definitions, procedures, crews, nominal quantification, recovery conventions, and dependency rules.", basis: "Baseline actions are retained or modified using fire-specific cues, indication failures, smoke, access, timing, workload, command transfer, and common-crew dependence.", transfers: hrTransfers }),
    makeInterface({ code: "F-IF-DA-IN", technicalElementCode: "DA", technicalElementName: "Data Analysis", direction: "INPUT", role: "Ignition-frequency, circuit-failure, reliability, and uncertainty parameters", payloadType: "DATA_PARAMETER", columns: ["Parameter class", "Population or failure mode", "Estimate", "Uncertainty model", "Applicability or adjustment"], description: "Data Analysis supplies qualified fire-event, circuit-failure, reliability, availability, and uncertainty parameters with applicability assessments.", basis: "Every parameter is linked to a source population, estimate, uncertainty model, affected fire records, and scenario-specific adjustment.", transfers: [...ignitionDataTransfers, ...circuitDataTransfers] }),
    makeInterface({ code: "F-IF-ESQ-OUT", technicalElementCode: "ESQ", technicalElementName: "Event Sequence Quantification", direction: "OUTPUT", role: "Fire scenario and event-sequence-family frequencies with uncertainty", payloadType: "FIRE_SCENARIO_RESULT", columns: ["Mean frequency", "5-95% interval", "Conditional probability", "Release category", "Operating states", "Result scope / cutsets"], description: "Internal Fire supplies quantified scenario and event-sequence-family results with uncertainty, release categories, operating-state scope, and traceable dominant cutsets.", basis: "Controlled F-R1.0 results are transferred on a plant-year basis after convergence, dependency review, uncertainty propagation, and independent reconciliation.", transfers: [...esqScenarioTransfers, ...esqFamilyTransfers] }),
    makeInterface({ code: "F-IF-RI-OUT", technicalElementCode: "RI", technicalElementName: "Risk Integration", direction: "OUTPUT", role: "Risk-significant Internal Fire contributors and importance measures", payloadType: "RISK_CONTRIBUTOR", columns: ["Rank", "Contributor type", "Absolute frequency", "Contribution", "FV and RAW", "Sequence family"], description: "Internal Fire supplies ranked PAUs, ignition sources, scenarios, equipment, cables, circuit failures, HFEs, and sequence-family contributors for plant risk integration.", basis: "Contributor values reconcile to the controlled Internal Fire result and use explicit hazard-origin ownership to prevent double counting with internal events, seismic, and flood risk.", transfers: riTransfers }),
  ];
  mef.integration.consistencyChecks = ["PAU_SOURCE", "EQUIPMENT_CABLE", "CABLE_RACEWAY", "SCENARIO_TARGET", "SCENARIO_FREQUENCY", "CIRCUIT_MODEL", "HFE_CONTEXT", "FREQUENCY_RECONCILIATION", "TRACEABILITY"].map((checkType, index) => ({ ...record(variant, `F-CHECK-${String(index + 1).padStart(2, "0")}`, `${checkType.replace(/_/g, " ")} consistency check`, "Reconciles linked records across the applicable Internal Fire technical subelements.", "Automated referential-integrity queries and independent sample tracing found no unresolved discrepancy.", [id(variant, "EVID-CONFIG")]), checkType: checkType as InternalFirePRA["integration"]["consistencyChecks"][number]["checkType"], subelements: ["FPP", "FES", "FCS", "FQLS", "FPRM", "FSS", "FIGN", "FCF", "FHR", "FESQ"], comparedRefs: [`CHECK-SOURCE-${String(index + 1)}`, `CHECK-DEST-${String(index + 1)}`], method: "Database join, count reconciliation, identifier comparison, and independent end-to-end trace sample.", result: "PASS" as const, openItems: [] }));
  mef.integration.integrationMethod = "Version-controlled interface manifests transfer baseline technical-element inputs to Internal Fire and controlled fire scenario, family, contributor, and uncertainty results to ESQ and Risk Integration; cross-hazard overlap is removed by mutually exclusive initiating-event and scenario ownership rules.";
  mef.integration.unresolvedInterfaces = [];
  mef.integratedUncertainties = subelementUncertainties.map((item, index) => ({ ...item, uuid: id(variant, `F-INTEGRATED-UNC-${String(index + 1).padStart(2, "0")}`), code: `F-INTEGRATED-UNC-${String(index + 1).padStart(2, "0")}`, name: `${item.sourceSubelement} uncertainty in integrated risk interpretation`, implementsSrs: [] }));

  mef.riskInterpretation.riskInsights = contributors.slice(0, 6).map((item, index) => ({ ...record(variant, `F-RISK-${String(index + 1).padStart(2, "0")}`, `Internal Fire risk insight ${String(index + 1)}`, `Interprets the significance of ${item.name.toLowerCase()} for design, fire defense in depth, configuration, operations, and model uncertainty.`, "Insight combines quantified importance, scenario mechanics, defense-in-depth review, uncertainty, and sensitivity results.", [id(variant, "EVID-PRA")]), insightType: index === 0 ? "DOMINANT_CONTRIBUTOR" as const : index === 1 ? "DEFENSE_IN_DEPTH" as const : index === 2 ? "UNCERTAINTY" as const : index === 3 ? "DESIGN_OPPORTUNITY" as const : "MODEL_LIMITATION" as const, contributorRefs: [item.uuid], affectedRiskMetric: context.principalMetric, fractionalContribution: item.fractionalContribution, decisionImplication: index === 0 ? "Protect the leading cable concentration and preserve diverse shutdown capability." : index === 1 ? "Maintain detection, suppression, barriers, and alternate shutdown as independent defense layers." : index === 2 ? "Retain the source-frequency and circuit-duration sensitivities in decision margins." : "Track the associated design or model action under configuration control." }));
  mef.riskInterpretation.refinementActions = ["Confirm final cable route", "Refine cabinet HRR distribution", "Update suppression reliability", "Reperform remote-shutdown timing", "Close barrier turnover evidence"].map((name, index) => ({ ...record(variant, `F-REFINE-${String(index + 1).padStart(2, "0")}`, name, `Controlled model refinement to ${name.toLowerCase()}.`, "The action targets a leading contributor or uncertainty and defines an expected quantified effect.", [id(variant, "EVID-CONFIG")]), technicalArea: ["CABLE", "SCENARIO", "SCENARIO", "HRA", "PARTITIONING"][index] as InternalFirePRA["riskInterpretation"]["refinementActions"][number]["technicalArea"], driverRefs: [contributors[index]!.uuid], affectedRecordRefs: [contributors[index]!.contributorRef], refinement: name, expectedEffect: "Reduce conservatism or confirm the controlled best-estimate basis without omitting credible fire risk.", priority: index < 2 ? "HIGH" as const : "MEDIUM" as const, refinementStatus: index < 4 ? "REQUANTIFIED" as const : "CLOSED" as const, quantificationIterationRef: id(variant, `F-ITER-${String(Math.min(3, index + 1)).padStart(2, "0")}`), result: "Refinement incorporated; aggregate frequency and leading contributor ranking remain stable.", decisionBasis: "Change is below the stopping criterion and creates no new risk-significant contributor." }));
  mef.riskInterpretation.quantificationIterations = [0, 1, 2].map((index) => ({ ...record(variant, `F-ITER-${String(index + 1).padStart(2, "0")}`, `Internal Fire refinement iteration ${String(index + 1)}`, "Records the controlled model version, aggregate result, family change, contributor stability, and refinement decision.", "Successive quantifications use identical convergence and uncertainty settings to support valid comparison.", [id(variant, "EVID-PRA")]), modelVersion: `F-R${index === 0 ? "0.8" : index === 1 ? "0.9" : "1.0"}`, calculationDate: `2026-06-${String(10 + index * 8).padStart(2, "0")}`, refinementActionRefs: mef.riskInterpretation.refinementActions?.slice(0, index + 2).map((item) => item.uuid) ?? [], aggregateMeanFrequencyPerPlantYear: [4.72e-6, 4.29e-6, 4.18e-6][index]!, previousAggregateMeanFrequencyPerPlantYear: index === 0 ? undefined : [4.72e-6, 4.29e-6][index - 1], relativeChange: index === 0 ? undefined : [0.091, 0.026][index - 1], maximumFamilyRelativeChange: [0.18, 0.082, 0.041][index]!, topContributorRefs: contributors.slice(0, 5).map((item) => item.uuid), contributorRankingStable: index > 0, newRiskSignificantContributorRefs: [], decision: index === 2 ? "ACCEPT_STABLE" as const : "CONTINUE_REFINEMENT" as const }));
  mef.riskInterpretation.stoppingCriteria = { maximumAggregateFrequencyChange: 0.05, maximumFamilyFrequencyChange: 0.1, maximumContributorRankShift: 1, requiredStableIterations: 2, requireNoNewRiskSignificantContributors: true, basis: "" };

  const aggregateMean = familyResults.reduce((sum, item) => sum + item.meanFrequencyPerPlantYear, 0);
  mef.riskIntegrationBaseline.results = [{ ...record(variant, "F-RI-RESULT-001", "Controlled Internal Fire annual risk result", "Transfers integrated Internal Fire event-sequence family frequencies, release categories, uncertainty, and contributors to Risk Integration.", "Result is produced by the approved F-R1.0 run and reconciled to every scenario and family total.", [id(variant, "EVID-PRA"), id(variant, "EVID-CONFIG")]), modelVersion: "F-R1.0", plantOperatingStateRefs: mef.baselinePra!.plantOperatingStateRefs, reactorUnitRefs: mef.baselinePra!.reactorUnitRefs, radioactiveMaterialSourceRefs: mef.baselinePra!.radioactiveMaterialSourceRefs, eventSequenceFamilyRefs: familyResults.map((item) => item.eventSequenceFamilyRef), releaseCategoryRefs: [...new Set(familyResults.map((item) => item.releaseCategoryRef))], aggregateMeanFrequencyPerPlantYear: aggregateMean, fifthPercentileFrequencyPerPlantYear: aggregateMean * 0.25, ninetyFifthPercentileFrequencyPerPlantYear: aggregateMean * 3.7, otherHazardRiskRefs: ["IE-RISK-BASELINE", "FL-RISK-BASELINE", "S-RISK-BASELINE"], overlapTreatment: "Internal Fire owns plant-internal combustion initiators; secondary fire from seismic is owned by Seismic PRA and excluded here, while fire-induced initiating events are removed from the internal-events aggregate before summation.", dominantContributorRefs: contributors.slice(0, 8).map((item) => item.uuid), integrationStatus: "READY_FOR_RISK_INTEGRATION" as const }];
  mef.riskIntegrationBaseline.decisions = ["Protect essential cable concentration", "Preserve remote shutdown habitability", "Control transient combustibles", "Maintain fire-door surveillance", "Collect plant fire experience"].map((action, index) => ({ ...record(variant, `F-DEC-${String(index + 1).padStart(2, "0")}`, action, `Risk-informed decision to ${action.toLowerCase()}.`, "Decision is supported by quantified contribution, defense-in-depth impact, uncertainty, and configuration-control screening.", [id(variant, "EVID-CONFIG")]), decisionType: ["DESIGN", "PROCEDURE", "FIRE_PROTECTION_PROGRAM", "CONFIGURATION_CONTROL", "DATA_COLLECTION"][index] as InternalFirePRA["riskIntegrationBaseline"]["decisions"][number]["decisionType"], driverRefs: [contributors[index]!.uuid, mef.riskInterpretation.riskInsights[index]!.uuid], affectedSscRefs: [context.equipment[index] ?? context.equipment[0]!], action, duePhase: index < 2 ? "Before fuel-load readiness" : "Before commercial operation", disposition: index < 2 ? "IMPLEMENT" as const : index === 4 ? "FORWARD_TO_PLANT_PROCESS" as const : "CONFIRM_PRE_OPERATIONAL" as const, verificationRefs: [`VERIFY-F-DEC-${String(index + 1)}`], reanalysisRequired: index < 2, riskIntegrationResultRef: id(variant, "F-RI-RESULT-001") }));
  mef.riskIntegrationBaseline.traceabilityPaths = scenarios.map((scenario, index) => ({ ...record(variant, `F-TRACE-${String(index + 1).padStart(2, "0")}`, `${scenario.name} end-to-end traceability`, "Traces controlled evidence through PAU, source, target, equipment, cable, circuit, plant response, HFE, family result, and risk decision.", "Automated links are reconciled to an independent manual trace of every retained scenario.", [id(variant, "EVID-CONFIG")]), evidenceRefs: [id(variant, "EVID-FHA"), id(variant, "EVID-CABLE"), id(variant, "EVID-PRA")], physicalAnalysisUnitRefs: scenario.physicalAnalysisUnitRefs, ignitionSourceRefs: scenario.ignitionSourceRefs, fireScenarioRefs: [scenario.uuid], equipmentRefs: targetSets[index]!.equipmentRefs, cableRefs: targetSets[index]!.cableRefs, circuitFailureRefs: [probabilities[index]!.uuid], initiatingEventRefs: scenario.initiatingEventRefs, humanFailureEventRefs: [hfes[index]!.uuid], eventSequenceFamilyRefs: scenarioResults[index]!.eventSequenceFamilyRefs, resultRefs: [scenarioResults[index]!.uuid, familyResults[index % familyResults.length]!.uuid], decisionRefs: [mef.riskIntegrationBaseline.decisions?.[index % 5]?.uuid ?? id(variant, "F-DEC-01")], complete: true }));
  mef.riskIntegrationBaseline.controlledBaselines = [{ ...record(variant, "F-BASELINE-001", "Internal Fire PRA F-R1.0 controlled baseline", "Configuration-controlled model, run, report, manifest, peer-review package, and known limitations released for approved use.", "Checksums, approvals, conformance, resolved findings, and ownership are verified before release.", [id(variant, "EVID-CONFIG")]), modelVersion: "F-R1.0", quantificationRunRef: runs[1]!.uuid, reportRef: "F-PRA-REPORT-R1.0", configurationControlRecordId: "CCR-F-2026-001", peerReviewRef: "F-PEER-REVIEW-2026", packageManifestRefs: ["F-R1.0-MANIFEST", "F-R1.0-CHECKSUMS", "F-R1.0-TRACEABILITY"], unresolvedLimitations: mef.metadata.limitations, releaseStatus: "CONTROLLED" as const }];
  mef.configurationControlRecordId = "CCR-F-2026-001";

  const workflowRecord = (code: string, name: string, workflowRecordType: InternalFirePRA["workflow"]["reportSections"][number]["workflowRecordType"], discipline: string, assignee: string, result: string) => ({ ...record(variant, code, name, `${name} is a controlled Internal Fire PRA workflow record.`, "Completion is verified against the report package, model manifest, conformance matrix, and review record.", [id(variant, "EVID-CONFIG")]), workflowRecordType, discipline, assignee, dueDate: "2026-08-15", result, verificationRefs: [`VERIFY-${code}`] });
  mef.workflow.reportSections = ["Scope and applications", "FPP through FQLS", "FPRM through FCF", "FHR and FESQ", "Risk insights and integration", "Conformance and configuration control"].map((name, index) => workflowRecord(`F-WF-REPORT-${String(index + 1).padStart(2, "0")}`, name, "REPORT_SECTION", index < 2 ? "Fire protection" : index < 4 ? "PRA modeling" : "Integration", ["A. Patel", "M. Chen", "J. Rivera", "S. Okafor"][index % 4]!, "Complete and cross-referenced"));
  mef.workflow.draftQualityChecks = ["Record completeness", "Calculation and unit check", "Reference and revision check", "Traceability query", "Conformance reconciliation", "Package manifest check"].map((name, index) => workflowRecord(`F-WF-QC-${String(index + 1).padStart(2, "0")}`, name, "QUALITY_CHECK", "Quality assurance", ["L. Morgan", "D. Shah"][index % 2]!, "Passed"));
  mef.workflow.reviewAssignments = ["Plant partitioning and fire protection", "Equipment, cable, and circuit analysis", "Plant response and quantification", "Fire HRA", "Integrated uncertainty and risk insights"].map((name, index) => workflowRecord(`F-WF-ASSIGN-${String(index + 1).padStart(2, "0")}`, name, "REVIEW_ASSIGNMENT", name, ["R. Kim", "T. Anders", "V. Iqbal", "E. Brooks", "C. Mensah"][index]!, "Review complete"));
  mef.workflow.reviewFindings = ["Clarify high-energy arcing source boundary", "Reconcile remote-shutdown cable route", "Document suppression common-cause sensitivity", "Confirm HFE dependency grouping"].map((name, index) => workflowRecord(`F-WF-FIND-${String(index + 1).padStart(2, "0")}`, name, "REVIEW_FINDING", ["FSS", "FCS", "FESQ", "FHR"][index]!, ["A. Patel", "M. Chen", "J. Rivera", "S. Okafor"][index]!, "Resolved and independently verified"));
  mef.workflow.approvalReadiness = ["Applicable SRs dispositioned", "Technical findings resolved", "Peer-review package accepted", "Controlled model and report match", "Limitations and ownership assigned"].map((name, index) => workflowRecord(`F-WF-READY-${String(index + 1).padStart(2, "0")}`, name, "APPROVAL_READINESS", "PRA governance", ["PRA Manager", "QA Manager"][index % 2]!, "Ready"));
  mef.workflow.approvalSignatures = [workflowRecord("F-WF-SIGN-01", "Responsible PRA manager approval", "APPROVAL_SIGNATURE", "PRA management", "N. Williams", "Approved for controlled use"), workflowRecord("F-WF-SIGN-02", "Independent technical authority approval", "APPROVAL_SIGNATURE", "Independent review", "K. Yamamoto", "Approved for controlled use")];
  mef.documentation = { overallProcessDescription: "The Internal Fire PRA follows the analyst workflow from scope and evidence through PAU partitioning, equipment and cable selection, qualitative screening, plant-response adaptation, scenario modeling, ignition frequency, circuit analysis, fire HRA, quantification, interpretation, integration, review, and controlled release.", partitioningSummary: `${String(paus.length)} PAUs cover the complete global boundary without overlap.`, equipmentSelectionSummary: `${String(equipment.length)} selected equipment records and ${String(instruments.length)} indications support modeled plant response.`, cableSelectionSummary: `${String(cables.length)} cables and ${String(raceways.length)} raceways provide spatial circuit traceability.`, qualitativeScreeningSummary: "Five PAUs retained and three screened using approved criteria.", plantResponseSummary: "Four event-sequence models and eight system modifications represent fire response.", scenarioAnalysisSummary: "Six retained scenarios cover principal scenario classes and detailed fire effects.", ignitionFrequencySummary: "Source-category data are apportioned to eight source-level estimates with exact total reconciliation.", circuitFailureSummary: "Eight circuits include loss, spurious operation, indication, hot-short, and duration effects.", humanReliabilitySummary: "Six fire HFEs include context, timing, feasibility, HEP uncertainty, recovery, and dependency.", quantificationSummary: "Six scenarios aggregate into four annual sequence-family results with converged uncertainty propagation.", riskInsights: "Dominant risk arises from essential electrical cable concentrations, high-energy electrical sources, remote-shutdown challenges, and correlated circuit/HFE effects; detection, suppression, rated separation, diverse shutdown, and configuration control provide the principal defense layers.", uncertaintySummary: "Ignition-frequency apportionment, fire growth and target damage, suppression timing, hot-short occurrence and duration, and fire HRA dominate uncertainty; quantified alternatives do not change the principal decisions.", configurationControlDescription: "The F-R1.0 model, report, data, calculations, interface manifests, traceability graph, conformance matrix, review record, and known limitations are controlled under CCR-F-2026-001.", peerReviewScope: "Independent peer review covers all ten Internal Fire subelements, integrated risk results, interfaces, uncertainty, configuration control, and pre-operational closure program.", supportingDocumentRefs: mef.evidenceRegister.map((item) => item.uuid) };
  mef.exampleDocuments = mef.evidenceRegister.slice(0, 10).map((item, index) => ({ id: item.uuid, name: item.name, kind: index % 3 === 0 ? "image" as const : index % 3 === 1 ? "sheet" as const : "doc" as const, sizeLabel: `${String(2 + index)}.${String(index % 10)} MB`, uploadedLabel: `Jun ${String(3 + index)}, 2026`, extracted: "Controlled technical attributes extracted and reconciled to linked records.", linked: 6 + index * 3 }));
  mef.newlyDevelopedMethodIds = [];
  return synchronizeInternalFirePraDerivedRegisters(mef);
}
