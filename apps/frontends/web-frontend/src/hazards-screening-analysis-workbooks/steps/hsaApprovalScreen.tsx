import { type JSX, type ReactNode, useMemo, useState } from "react";
import { POSIcon } from "../../pos-workbooks/posIcons";
import { Badge } from "../../pos-workbooks/posShared";
import { WorkbookSectionHeading } from "../../workbooks/workbookSectionHeading";
import { hsaConformanceItems, hsaConformanceScore } from "../hsaConformance";
import { type HsaPersona } from "../hsaWorkbench";
import { useHsaWorkbook } from "../hsaWorkbookContext";
import { type HsaWorkflowActions } from "./hsaDraftScreen";
import { HsaReviewBanner, HsaReviewComments } from "./hsaReviewScreen";
export function HsaApprovalScreen({ persona, actions, renderApprovalTable, renderSignCard }: { persona: HsaPersona; actions?: HsaWorkflowActions; renderApprovalTable?: () => ReactNode; renderSignCard?: () => ReactNode }): JSX.Element { const { mef } = useHsaWorkbook(); const [handoff, setHandoff] = useState<string | null>(null); const score = useMemo(() => hsaConformanceScore(hsaConformanceItems(mef)), [mef]); const comments = mef.internalReviewComments.comments; const resolved = comments.filter((comment) => comment.resolved).length; const approved = mef.workflowState === "FINAL"; const snapshot = mef.configurationControlRecordId ?? ""; return <><HsaReviewBanner /><HsaReviewComments comments={comments} persona={persona} actions={actions} approvalView title={persona === "preparer" ? "Comments by reviewers & approvers" : "Your comments"} description="Reviews the comment record supporting the HSA approval decision, including every disposition and responsible role." /><div className="poscard"><div className="poscard__head"><WorkbookSectionHeading workbook="HSA" title="What is being attested" description="Confirms the capability target, supporting-requirement status, resolved comment record, final hazard disposition register, accepted handoffs, and controlled configuration snapshot covered by approval." /></div><div className="posapprove__attest-with-sign"><div className="posapprove__attest-grid"><div className="posapprove__attest-row"><span className="posapprove__attest-cap">Capability target</span><span className="posapprove__attest-val"><strong>{mef.capabilityCategory}</strong> · Hazards Screening Analysis</span></div><div className="posapprove__attest-row"><span className="posapprove__attest-cap">Items satisfied</span><span className="posapprove__attest-val posmono">{score.met} of {score.applicable}</span></div><div className="posapprove__attest-row"><span className="posapprove__attest-cap">Hazard dispositions</span><span className="posapprove__attest-val posmono">{mef.integration.finalDispositions.length} of {mef.hazardInventory.hazards.length}</span></div><div className="posapprove__attest-row"><span className="posapprove__attest-cap">Accepted handoffs</span><span className="posapprove__attest-val posmono">{mef.integration.technicalHandoffs.filter((item) => item.acceptanceStatus === "ACCEPTED").length} of {mef.integration.technicalHandoffs.length}</span></div><div className="posapprove__attest-row"><span className="posapprove__attest-cap">Review comments</span><span className="posapprove__attest-val posmono">{resolved} of {comments.length} resolved</span></div><div className="posapprove__attest-row"><span className="posapprove__attest-cap">Configuration snapshot</span>{snapshot.length > 0 ? <span className="posapprove__attest-val poscclink"><POSIcon.Lock /> {snapshot}</span> : <span className="posapprove__attest-val possubtle">Not linked</span>}</div></div>{renderSignCard !== undefined && <div className="posapprove__sign-col">{renderSignCard()}</div>}</div></div>{renderApprovalTable?.()}{approved && <div className="poscard posapprove__handoff"><div className="poscard__head"><WorkbookSectionHeading workbook="HSA" title="After approval — external workflows" description="Releases the approved, locked HSA baseline to independent peer review or audit with view-and-comment access only." /><Badge kind="draft">View + comment only</Badge></div><div className="posapprove__handoff-grid"><button type="button" className="posapprove__handoff-card" onClick={() => setHandoff("Peer Review release selected")}><div className="posapprove__handoff-card-head"><div className="posapprove__handoff-card-icon"><POSIcon.Eye /></div><div><div className="posapprove__handoff-card-eyebrow">External · HSA</div><div className="posapprove__handoff-card-title">Peer Review</div></div></div><div className="posapprove__handoff-card-foot"><span>Release to peer review</span><POSIcon.ArrowR /></div></button><button type="button" className="posapprove__handoff-card" onClick={() => setHandoff("Audit release selected")}><div className="posapprove__handoff-card-head"><div className="posapprove__handoff-card-icon"><POSIcon.Lock /></div><div><div className="posapprove__handoff-card-eyebrow">External · NQA-1 aligned</div><div className="posapprove__handoff-card-title">Audit</div></div></div><div className="posapprove__handoff-card-foot"><span>Release to audit</span><POSIcon.ArrowR /></div></button></div>{handoff !== null && <p className="posmuted" role="status">{handoff}</p>}</div>}</>; }
