{
  "name": "scram-node",
  "version": "0.0.1",
  "license": "AGPL-3.0-or-later",
  "main": "./lib/index.js",
  "scripts": {
    "install": "cmake-js -C --parallel 32 compile --target scram scram-node ",
    "rebuild": "cmake-js -C --parallel 32 rebuild --target scram scram-node ",
    "configure": "cmake-js -C --parallel 32 configure --target scram scram-node ",
    "clean": "cmake-js clean",
    "test": "jest",
    "ctest": "cmake-js -C --parallel 32 install -T test_mef"
  },
  "nx": {
    "namedInputs": {
      "default": [
        "{projectRoot}/**/*"
      ],
      "production": [
        "!{projectRoot}/**/*.spec.tsx"
      ]
    },
    "targets": {
      "build": {},
      "clean": {},
      "distclean": {},
      "build-deps": {},
      "tidy": {},
      "format": {},
      "coverage": {},
      "debug": {},
      "release": {},
      "sanitize": {},
      "test": {
        "inputs": [
          "default",
          "^production"
        ],
        "outputs": [],
        "dependsOn": [
          "build"
        ]
      }
    }
  },
  "dependencies": {
    "cmake-js": "7.2.1",
    "node-addon-api": "7.0.0"
  },
  "devDependencies": {
    "jest": "29.7.0"
  },
  "binary": {
    "napi_versions": [7]
  },
  "engines": {
    "node": "20.9.0"
  }
}
