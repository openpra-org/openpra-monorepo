import { TemplatedPageHeader } from "../../components/headers/TemplatedPageHeader";

const CcfGroups = (): JSX.Element => {
  return (
    <TemplatedPageHeader
      pageTitle="Common Cause Failure Groups"
      iconType="tokenShape"
    />
  );
};

export { CcfGroups };
