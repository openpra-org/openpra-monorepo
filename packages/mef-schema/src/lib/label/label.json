{
  "$schema": "https://json-schema.org/draft-07/schema#",
  "$id": "label.json",
  "title": "Label",
  "description": "Contains the name and description fields",
  "type": "object",
  "properties": {
    "name": {
      "$ref": "name.json"
    },
    "description": {
      "$ref": "description.json"
    },
    "uuid": {
      "$ref": "../identifier/uuid.json"
    }
  },
  "required": ["name", "description"],
  "examples": [
    {
      "name": "Urgent",
      "description": "This label is for tasks that need to be done immediately"
    },
    {
      "name": "Low Priority",
      "description": "This label is for tasks that can be done later"
    }
  ]
}
