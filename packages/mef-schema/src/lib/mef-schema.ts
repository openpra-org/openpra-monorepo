/**
 * Placeholder function for the `mef-schema` package public surface.
 *
 * This package primarily ships generators and artifacts; this function exists
 * to satisfy tooling and may be replaced with real exports as the package evolves.
 */
export function MefSchema(): string {
  return "mef-schema";
}
