/**
 * Example export for the model-generator package.
 *
 * Replace with real APIs as the package evolves.
 */
/**
 * Example API for the model-generator package.
 *
 * @param name - Optional name to include in the greeting (for docs coverage example).
 * @returns The static module identifier.
 */
export function ModelGenerator(name?: string): string {
  void name; // intentionally unused; preserved for demonstration
  return 'model-generator';
}
