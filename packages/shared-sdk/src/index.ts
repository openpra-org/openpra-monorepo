/**
 * OpenPRA Shared SDK runtime exports.
 *
 * This package provides browser/runtime helpers for authentication, API access,
 * and role/invite utilities. It re-exports stable, public APIs from `src/lib`.
 *
 * Consumers should import from `shared-sdk` rather than deep-linking into files.
 * @packageDocumentation
 */
export * from "./lib/shared-sdk";
