/**
 * Internal helper used by the shared-types package tests.
 *
 * Not intended as a public API; kept for historical parity with Nx generators.
 * @returns A sentinel string identifying the package.
 */
export function SharedTypes(): string {
  return "shared-types";
}
