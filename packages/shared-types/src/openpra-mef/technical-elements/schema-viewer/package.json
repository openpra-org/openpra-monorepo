{
  "name": "schema-viewer",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "serve": "http-server dist -o -c-1",
    "watch": "nodemon --watch ../../ --ext ts --exec 'npm run build'"
  },
  "devDependencies": {
    "typescript": "^4.9.5",
    "typescript-json-schema": "^0.62.0",
    "http-server": "^14.1.1",
    "nodemon": "^3.0.3"
  }
} 